# Checkpoint 01 — 2026-06-29 (S1)

## Qué cubrimos
- Diagnóstico inicial: el cockpit **ya** tiene el modelo `Capability`/`Story`/registry `Kind=factory|own|client`
  que el operador quiere — el gap es que **prenter-harness nunca se instaló sobre sí mismo** (zapatero
  descalzo). El corte clase/objeto ya existe = **el seam** (`project.config.yaml`).
- Decidimos ubicación del dogfood-objeto: **in-repo enrejado**, prenter lo posee vía registry (D-01).
  Descartado `prenter` (caja EMPRESA, máximo split-brain) y repo separado (split-brain de código vs specs).
- Modelo de **sistema**: `rol` (soporte|producto, dual opt-in) + `audiencia` (interno|cliente-final) (D-02).
  Corrección: frontstage/backstage ≠ tipo de sistema; "externo" ya está ocupado por procedencia.
- **Salvedad arquitectónica** del operador: normalizar el negocio → un-archivo-por-entidad, hexagonal,
  alta cohesión/bajo acoplamiento, detrás de un **puerto** (archivos hoy, DB diferido) (D-03, D-04).
- **Organización**: cadena `persona → rol → funciones → proceso` (D-05). El rol es el ancla.

## Dónde paramos
A punto de responder **Pregunta 2 (el "manual")**. Ver `NEXT-PROMPT.md`.

## Bancado (no perder)
- función archivo-vs-inline · persona:rol N:M (Criscore = 1 persona muchos roles) · audiencia "ambos".
- Analytics de producto (adopción/uso) = futuro sobre lente `producto`.
- Vitalia/Comunify automatizan solo venta+pagos internamente → insumo "base mínima".

## Nota de método
Registro CTO→CEO. Entrevista 1-pregunta-a-la-vez. Nada de código hasta cerrar el modelo.
