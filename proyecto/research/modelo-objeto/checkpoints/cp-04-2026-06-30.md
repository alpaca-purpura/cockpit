# CP-04 · 2026-06-30 — Investigación 4-subagentes → estructura inicial v0

**Macropaso:** M1 (modelar el OBJETO) — cerca del cierre. **Deliverable:** `ESTRUCTURA-INICIAL.md`.

## Qué se hizo
Investigación real con **4 subagentes en paralelo** + síntesis:
1. **TOGAF/ArchiMate** — metamodelo: 8 entidades first-class, 6 aristas portantes, capa Motivación
   (objetivo=Goal, brecha=Assessment), función=skip, sistema≠producto. + 4 tests de desambiguación.
2. **Cross-norma (ISO 9001/9000·APQC·BPMN·RACI·OKR)** — set de campos auditable por entidad: proceso = ISO
   4.4.1 a–h + tortuga; objetivo = ISO 6.2 + OKR triple; brecha = nonconformity|target_variance; APQC L1..L5.
3. **DDD/normalización** — aggregate roots vs embebidos, reglas FK (un-hecho-un-lugar, inverso por scan),
   layout, mapeo a tablas. empresa=tenant root, proceso/objetivo/sistema embeben weak-entities.
4. **Auditoría interna** — EL hallazgo: ya existe `negocio.schema.yaml` (prototipo) + backbone **O1–O7**
   sellado + cadena **Hilo de Oro** = aristas + puerto-archivo ya 3×. Conflictos internos a reconciliar.

## Hallazgo que reordena
**No inventamos: extendemos `negocio.schema.yaml`, anclando a O1–O7, con el Hilo de Oro como modelo de aristas.**
Los 4 frentes convergen en las **mismas 8 entidades + mismos roots**.

## La estructura v0 (ver doc)
8 entidades: `empresa`(root/tenant) · `persona`·`rol`(O4, **NET-NEW**) · `proceso`(O3, tortuga+actividades inline) ·
`sistema`(O5, App Component + Product opt-in) · `objetivo`(O2, **split KR**) · `capability`(O5, TOGAF) ·
`brecha/gap`(O6, unifica). provenance (`fuente·conf`) obligatorio en todo AS-IS.

## Forks a confirmar (Pregunta 5)
A anclar O1–O7 · B split objetivo/KR · C unificar gap (brecha=vista) · D persona/rol net-new ·
**E ⚠ colisión `audiencia`** (negocio vs product-paradigm copilot/sales_agent → renombrar) ·
F sistema = una App Component (no 4 sentidos) · G capability negocio ≠ producto (enlazadas por realizes).

## Confirma D-08/09/10
- D-08 (proceso tortuga + actividades inline) ✓ los 4. D-09 (función disuelve) ✓ TOGAF (skip función).
- D-10 (sistema App Component + Product) ✓ TOGAF — pero abre Fork F (4 sentidos de `sistema`) y Fork E (`audiencia`).

## Siguiente
- **Pregunta 5:** confirmar forks A–G → al confirmar, **trigger de materialización** alcanzado (grafo estable)
  → arrancar `negocio.schema.yaml` extendido en `.claude/harness/schema` + guía + dogfood.
- Glosario actualizado: colisión `audiencia` abierta; capability ISO-vs-TOGAF anotada.
