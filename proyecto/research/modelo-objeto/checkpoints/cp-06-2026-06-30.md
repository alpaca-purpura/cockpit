# cp-06 · 2026-06-30 — `area` entidad (D-12) · `negocio.yaml`=generado (D-13) · dogfood listo

## Qué se hizo
Arrancó la **Pregunta 6** (iterar sobre v1). El operador eligió **dogfood Proyecto #0** — pero ANTES de
instanciar, preguntas suyas destaparon hallazgos que refinaron el modelo **en seco**. El dogfood NO se
escribió todavía: va en una **conversación nueva** (pedido del operador).

## Hallazgos + decisiones
1. **Dueño del dogfood = `~/Proyectos/prenter`** (la EMPRESA, sibling). prenter ya modela a prenter-harness
   como `empresa.yaml::sistema_principal` (kind=factory, holding=chris-corp). → el dogfood in-repo NO es un
   shell-empresa: es el **plano-proyecto/sistema** de prenter-harness, propiedad de la cuenta `prenter` por
   slug (D-01/I-39).
   - **2 hallazgos del modelo (pre-escritura):** (a) v1 confundió `empresa = partición = repo`; la topología
     real es cuenta(sibling)→proyecto(este repo). (b) `un-hecho-un-lugar` (inverso por scan) **muere en el
     borde de repo** → cross-repo exige ancla slug explícita (I-39). [Abierto: el seam `cuenta_ref`.]
2. **empresa.yaml vs negocio.yaml vs marketing = 3 concerns, no 2** (investigado con data, ambos repos):
   - **Marca** → `marketing/source/` (lo lee el skill `crear-pieza-comercial`; NO lee ninguno de los dos archivos).
   - **Diagnóstico** → `negocio.yaml` (lo lee el cockpit, `handlers_negocio.go:45`).
   - **Identidad/estructura** → `empresa.yaml` (NINGÚN programa lo lee; doc curado para humanos/agentes).
   - Redundancia menor: `empresa.yaml` copia un slice de marca (tagline/contacto) cuyo SSoT es `marketing/` → dejar ref pura.
3. **D-13 · `negocio.yaml` = GENERADO** (proyección), no poblado. Mapa campo-por-campo `negocio.schema→objeto`:
   ~90% deriva limpio (`obj`/`puesto`/`brecha.tipo`/`brecha.obj` = inversos-FK/Hilo). La población M1 se mueve
   **upstream a las entidades**; negocio.yaml deja de poblarse. Mecanismo (archivo vs in-memory) = diferido a D-04.
4. **D-12 · `area` = entidad** (Org Unit / ArchiMate Business Actor org.), **revierte ESTRUCTURA §1**. Cierra el
   único hueco del mapa de (3). **N:M con proceso** (transversalidad), owning side `proceso.areas_ref[]`.
   `lider_ref→rol`, `parent_ref→area`. `rol.area_ref` NO se agrega (YAGNI). Niveles = `clasificacion` APQC +
   `actividad.carril`. **Modelo 8→9 entidades.**

## Artefactos tocados (verificado: parse + integridad de refs OK)
- `.claude/harness/schema/objeto.schema.yaml` — nodo `area` + `proceso.areas_ref[]` + 3 relaciones + invariante.
- `campaign/ejemplo-vertice.yaml` — `areas[]` (finanzas, comercial) + `pr-cobranza` transversal (areas_ref).
- `DECISIONES.md` — D-12, D-13.

## Opens que NO frenan (se clavan con el dogfood, data real)
- **Seam `cuenta_ref: prenter`** (cross-repo) — la raíz del plano-proyecto del dogfood.
- **¿El `objeto` absorbe el org-structure?** (`holding`/`sistema_principal`/socios/líneas/legal de `empresa.yaml`)
  — o queda identidad curada fuera del grafo. Lean previo: `sistema_principal`/`holding` SÍ (relaciones/seam);
  socios/legal/líneas = identidad curada.

## Próximo (conversación nueva)
**Dogfood Proyecto #0** = rebanada vertical del Hilo (objetivo→KR→proceso→rol/persona/**area**→sistema con
`producto{}`→capability→brecha) de prenter-harness, ancla `cuenta_ref: prenter`, ejercitando el bloque
`producto{}` que el Vértice ficticio nunca tocó. Ver `NEXT-PROMPT.md` (Pregunta 7).
