# docs/architecture/ — lecturas humanas de arquitectura

Documentos de arquitectura **para leer** (principio hexagonal, CK-28): nada de aquí lo valida un
gate. La arquitectura VIGENTE es as-code y vive en
[`sistema/arquitectura/`](../../sistema/arquitectura/) — SSoT `NODOS.md` (16 nodos) +
`arquitectura.yaml` (componentes) → vistas generadas + `despliegue.html` validado; regla:
`.claude/rules/arquitectura-as-code.md`.

| Qué | Dónde |
|---|---|
| [`forma-de-trabajo/ARQUITECTURA.md`](./forma-de-trabajo/ARQUITECTURA.md) | Arquitectura de la FORMA DE TRABAJO (proceso de desarrollo, ejes as-code, cerebro, runtime, memoria) — snapshot para el operador; ante divergencia mandan las fuentes que cita. |
| [`producto/ARCHITECTURE.md`](./producto/ARCHITECTURE.md) | Visión CTO del PRODUCTO/ecosistema — **histórica** (banner pre-CK-18): contexto de cómo se llegó al modelo Fábrica + Organización + Edge. El cableado vigente es `sistema/arquitectura/NODOS.md`. |
| [`producto/ck-02-stage1-diagram.html`](./producto/ck-02-stage1-diagram.html) | Diagrama histórico de la extracción física Stage 1 (pre-Stage-4). |
