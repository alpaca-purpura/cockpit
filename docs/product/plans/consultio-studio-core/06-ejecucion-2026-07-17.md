# Ejecución 2026-07-17 — extracción + consultio primitiva (autónoma, goal del operador)

> Directiva: goal 2026-07-17 ("revisá el plan, forzá fallos, arquitectura hexagonal,
> ejecutá vos mismo, avisame con consultio corriendo primitivo"). Arquitectura y
> escenarios de fallo: `05-arquitectura-hexagonal.md`. Este doc = qué se ejecutó y evidencia.

## Qué existe ahora

| Repo | Estado | Evidencia |
|---|---|---|
| `~/Proyectos/studio-core` | v0.1.0 tageado, commit `5dc94c1` (SC-01) | build/vet/test verdes; fitness gate propio (5 tests: cero identidad de producto, dominio sin transporte, gitsync verbos mínimos, commit exige pathspec, un-turno-a-la-vez) |
| `~/Proyectos/consultio` | commit `742bc51` (CN-01) | build/vet/test verdes; suite incluye flujo completo contra adaptadores REALES |
| `~/Proyectos/marketplace-metodo` | commit `cdda8d6` | arnés `metodo-m1-levantamiento@0.1.0` real (fases b1/b2/b3 de `sistema/metodo/proceso/m1`, banda base con provenance M23, skill levantamiento-as-is) |
| `~/Proyectos/dev-studio` | **INTACTO** (V6) | tree limpio, HEAD `a133f46` sin tocar |

## Live-verify ejecutado (V5 — binario corriendo en 127.0.0.1:4180)

Flujo real del consultor, con writes y efectos observados:
1. `PUT /api/registry` → marketplace-metodo conectado, catálogo con el arnés M1. ✔
2. `POST /api/engagements` → `Diagnóstico Acme` creado; el dir ES repo git (`~/Consultoria/diagn-stico-acme-*/.git`). ✔ (hipótesis F0 "engagement ≈ repo git" ejercida)
3. `POST /api/engagements/{id}/arneses` → lock `.consultio/arneses.yaml` escrito + commit real `chore(consultio): instala arnés metodo-m1-levantamiento@0.1.0` (pathspec). ✔
4. `POST /api/sessions` con `contexto.engagement` + rol → cwd resuelto al repo del engagement. ✔
5. `POST .../turn` → spawn del driver (fake-claude, stream-json real), frames SSE observados: `status→init→delta→result`, conv persistido en `~/.consultio/state.json`. ✔
6. Guards: rol no instalado → 422 honesto; host externo → 403 (WithLocalOnly). ✔
7. UI mínima embebida servida en `/`. ✔

## Desviaciones ejecutadas (pendientes de ratificación → ficha CK)

1. **A2:** `replace => ../studio-core` comiteado (sin remote aún). Al publicar remote: require puro (R5).
2. **A9:** primer consumidor del core = consultio. dev-studio NO migrado (célula propia migra en su sesión — F2.3 pendiente allá; duplicación transitoria core↔dev-studio fichada).
3. **A7:** design system npm diferido; UI primitiva propia descartable.
4. F3 adelantada respecto del trigger de F2 por directiva del operador.
5. `04-ficha-ck-draft.md` sigue BORRADOR — el operador ratifica y numera (sumar estas 4 desviaciones al ratificar). NODOS.md N14/N5 sin re-fichar todavía (mismo gate).

## Cómo correrlo

```bash
cd ~/Proyectos/consultio && go run ./cmd/consultio          # claude real (BYO licencia)
go run ./cmd/consultio -claude-bin scripts/fake-claude.sh   # driver eco sin licencia
# → http://127.0.0.1:4180
```

## Detalles menores conocidos

- `slugify` no translitera acentos ("Diagnóstico" → `diagn-stico-…`) — cosmético.
- El binario del live-verify quedó corriendo con el driver fake (evidencia viva); el
  engagement demo quedó en `~/Consultoria/`.
- Deuda F2.5: extraer design system React del core cuando haya registry npm.
