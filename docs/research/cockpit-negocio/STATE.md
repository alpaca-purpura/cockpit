# STATE — realidad actual (handoff para sesión fría)

> Snapshot al cierre de **Fase 5 slice 2** (2026-06-27) — **Vista de Negocio COMPLETA**. Actualízalo al cerrar cada fase. Memoria persistente relacionada: `[[portfolio-holding-model]]`, `[[cockpit-business-tool-campaign]]` (índice en `~/.claude/.../memory/MEMORY.md`).

> 🔴 **RENAME GRANDE (rebrand `ed8ab81`, ejecutado por el operador a mitad de Fase 5):** el repo de la fábrica se renombró **`alpaca-harness` → `prenter-harness`** y se MOVIÓ a **`~/Proyectos/prenter-harness`** (`~/Proyectos/alpaca-harness` **ya no existe**). El remoto (`penter`→`prenter-harness`, typo corregido), el módulo Go (`…/prenter-harness/cockpit-go`), PRODUCT-VISION y gen_ledger ya migrados en el rebrand. El dir de memoria migró también (`-prenter-harness`). El sistema-fábrica de Prenter pasó de slug `alpaca-harness` a **`prenter-harness`** (reconciliado en Fase 5 — el rebrand había dejado su `workspace` en path muerto).

## Repos y su estado git (al cierre de Fase 5 slice 2)
| Repo | Ruta | Rama | HEAD | GitHub | Limpio |
|---|---|---|---|---|---|
| **prenter-harness** (ex alpaca-harness) | `~/Proyectos/prenter-harness` | main | `4ee76db` | `alpacapurpura/prenter-harness` · ✅ pusheado (C-resto-(a)/I-51 nav per-board: `4ee76db`; antes A/I-50 deep-link `9560154`) | sí |
| luana-platform | `~/Proyectos/luana-platform` | main | `281242df` | `luana-platform` · ✅ pusheado · **sin cambios en F5** (el `nav:` NO tiene `negocio` — ver nota) | sí (salvo untracked ajenos `.claire/.clone/tools/`) |
| PeruSaaS | `~/Proyectos/PeruSaaS` | main | `009bdef` | `perusaas` · ✅ pusheado | sí |
| chris-corp | `~/Proyectos/chris-corp` | main | `fd4565d` | `chris-corp` · ✅ pusheado (F5 slice 1: proyecta `repo` + slug/workspace prenter-harness) · **sin cambios en slice 2** | sí |
| prenter | `~/Proyectos/prenter` | main | `df57c68` | `prenter` · ✅ pusheado (F5 slice 2: `brechas[]` + `ciclo` + huérfano en `empresa/negocio.yaml`) · gate propio (check.py) | sí |
| vitalia | `~/Proyectos/vitalia` | main | `4f56b18` | `vitalia` · ✅ en sync · sin cambios (negocio.yaml ausente = empty-state honesto) | sí |
| nicolify | `~/Proyectos/nicolify` | main | `6a68e1a` | `nicolify` · ✅ en sync · sin cambios | sí |
| comunify | `~/Proyectos/comunify` | main | `10e4734` | `comunify` · ✅ en sync · sin cambios | sí |

> ✅ **Flota en sync con origin (2026-06-28, tras E·Fase 3c/I-52)** (prenter-harness `a826f6f` [kit rename + shim] · prenter `6634372` · chris-corp `6255db4` · PeruSaaS `3a74e58` [los 3 seams→`sistemas:`] · luana `32af60ae` [**queda en `brands:` por diseño** — su seam = 23.5K occ; el shim lo puentea]; vitalia/nicolify/comunify sin cambios). Working tree limpio. `demo-environment` no es repo.

> 🚚 **Launcher del multi-cockpit MOVIDO a chris-corp (2026-06-27, post-limpieza · home base I-48):** `cockpit-daemon.sh`+`cockpit-up.sh` salieron de `luana/scripts/` (estaban ahí por accidente histórico) → `chris-corp/harnesses/scripts/` (chris-corp `107d606` move + `e38706f` fix de portabilidad: el `check_cockpit_version` asumía `project.config.yaml`, ausente en chris-corp, y mataba el arranque bajo `set -e` → `|| true`). luana `32af60ae`: borrados los targets `cockpit-up/down/status/restart/multi/multi-down` del Makefile (single-mode DESCARTADO; ya solo multi). **Verificado en vivo:** el multi-cockpit levanta desde chris-corp (`:4000` HTTP 200, pid/log en `chris-corp/.cockpit/`). **Comando (post-remate B):** `make cockpit-up` desde chris-corp (multi-only · :4000). El remate fino se CERRÓ en la sesión chris-corp (B/I-48 · commits `db9776e`/`c885e03`/`1f45b4f`/`978c0c3`): branding **Prenter Cockpit**, Makefile, **poda del single-mode** (cockpit-up.sh + branch single + detección per-worktree + gate COCKPIT_MULTI), doctrina § Cockpit + skill `orientacion-portfolio` + de-stale alpaca→prenter del repo.

## 🧹 Limpieza de fábrica (2026-06-27, post-Fase-5) — cerrada
Sesión de cierre de pendientes CHICOS de la fábrica (cada item = su micro-ciclo + commit independiente). Todo pusheado.
- **ITEM 1 ✅ (`57d09c4`)** — bug latente platform-pseudo CORREGIDO: 6 handlers de lectura (+ GET del mixto cap-single) pasan `getSistemas`→`getSelectableSistemas`; el PATCH sigue rechazando platform. Era latente (Map/Drift ya cortaban `isPlatform` en su `load()` → no alcanzable navegando, sí por API). 8 tests de regresión. Anclado a I-40 (no I-48). **El ⚠ de abajo queda RESUELTO.**
- **ITEM 2 ✅ (`8b2c9ba`)** — el filtro de objetivo (`activeObj`) se RECUERDA por empresa en `NegocioView` (memoria-en-sesión vía `useRef`; no sobrevive recarga → sin bookmarks fantasma).
- **ITEM 3 ✅ (`1e4f64f`)** — `negocio.yaml` BLINDADO: contrato `negocio.schema.yaml` (L0, 5º schema) + validación al leer en el cockpit (`handleNegocio`→`validateNegocio`→`warnings[]` no-fatales + banner ámbar en la Vista de Negocio). El cockpit = chokepoint de todo negocio.yaml → un punto cubre todas las empresas. Convención nueva `instancias_externas:true` en la fitness function para contratos cross-repo (H-17).
- **ITEM 4 ⏸ DIFERIDO** — skill marketing portable: SILLA EQUIVOCADA. Verificado: `crear-pieza-comercial` ya NO está en la fábrica (I-39 lo migró a la EMPRESA; hoy 4 copias en prenter/vitalia/nicolify/comunify, 0 en fábrica). Handoff a sesión prenter/empresa (ver Pendientes). Memoria [[marketing-skill-portability-debt]] corregida.
- **ITEM 5 ✅ (`edbea40`)** — ledger: **I-48** (chris-corp = home base + skill `orientacion-portfolio`, `decidida`/construcción diferida) · **I-49** (blindar negocio.yaml, ejecutada) · **H-17** (instancias_externas cross-repo). 49 fichas. Próximo id libre = **I-50**.

> ⚠ **El conteo de la fábrica bajó 9→8 puertos/skills** (no es regresión): `crear_pieza_comercial`/`crear-pieza-comercial` MIGRÓ a prenter por I-39. La memoria [[harness-factory-architecture]] decía 9 (point-in-time) → corregida a 8 + 5º schema (negocio) + H-17.

> ⚠ **El tab "Negocio" NO se muestra en el host `:4000`** (`luana-platform/cockpit.config.yaml` `nav:` no incluye `negocio` — se revirtió en sesión, intencional; NO se commiteó luana). La **vista en sí está verificada en vivo** (renderizó durante el smoke). Para verla en `:4000`: agregá `negocio` a esa lista a mano. El binario default (sin `nav:`) sí trae el tab.

> ✅ **Bug platform-pseudo RESUELTO** (Limpieza de fábrica ITEM 1, `57d09c4`): `/api/system-map` · `/api/value-stream` · `/api/capabilities` (+ cap-status/code-index/bidirectional/doctor + GET de cap-single) ahora validan con `getSelectableSistemas()` → aceptan `platform`. Era latente (las vistas Map/Drift ya cortaban `isPlatform` en su `load()` antes de fetchear → no alcanzable por navegación, sí por API directa). Los handlers de ESCRITURA siguen rechazando platform. Tests de regresión en `handlers_platform_pseudo_test.go`.

## ⚠ Rename brand→sistema EJECUTADO en el cockpit (I-43) — IMPACTA EL TOOLCHAIN
El cockpit dejó de llamar "brand" al concepto: **ahora es `sistema`** (la UI ya decía "Sistema"; el código se alineó). Lo que cambió y el próximo dev debe saber:
- **Wire:** el query param es `?sistema=` (NO `?brand=`). Endpoint `/api/sistemas` (NO `/api/brands` → 404).
- **Env:** `DEFAULT_SISTEMA` (NO `DEFAULT_BRAND`); hub = `cross-sistema`. Los scripts de luana (`cockpit-up.sh`/`cockpit-daemon.sh`) ya lo usan.
- **Código:** `resolveSistema`, `SistemaProvider`/`useSistema`, `getSistemas`, etc. En el provider: `sistema` = key navegable activa (lo que leen las vistas) vs `sistemaSlug` = slug elegido en el dropdown (puede ser gap).
- **DIFERIDO (deuda, ficha I-43):** el rename NO tocó el **kit core-harness + starters** (~280 archivos destilados, "brand" mezclado con marca-marketing, breaking = bump mayor). El **seam `project.config.yaml` sigue con la key legacy `brands:`** (el cockpit la lee como tal; comentado en `workspace.go`/`project-config.ts`). Esa migración es su propia sesión.

### Empresa-shells de las SaaS luana (construidos 2026-06-27, post-Fase-0)
Decisión del operador: cada empresa (Vitalia/Nicolify/Comunify) = repo propio = **shell de negocio liviano** (NO kit SDD; el SaaS-código vive en el worktree luana). Dominios: `empresa/` (socios/objetivos/RUC), `marketing/` (GTM propio: `source/` SSoT stub + `collateral/` + `methods/` + skill `crear-pieza-comercial` copiada de la fábrica), `sistemas/` (referencia al worktree). **Wiring del registry:** `repo`→shell, `cockpit.path`→worktree (board navegable INTACTO; `repo` no se proyecta a cockpit.yaml). Plantilla reusable quedó en `scratchpad/empresa-template/` (efímera). prenter NO tiene `marketing/` a propósito (su marketing = el de la fábrica). **PeruSaaS pendiente:** mismo patrón (su repo es el storefront-sistema, no un shell) — no hecho aún.

⚠ **Pendientes de Fase 0 (sueltos, no bloquean Fase 1):**
- Los 3 repos existentes están **1-ahead** (commiteados, no pusheados — fue deliberado: el operador pidió "commitear" esos vs "crear y pushear" los nuevos). Decidir si se pushean.
- Remoto de alpaca-harness = `penter-harness` (¿typo de "prenter-harness"? revisar/renombrar).
- luana tiene untracked **ajenos** a la campaña: `.claire/ .clone/ tools/` (preexistentes; dejados intactos; ¿gitignore o limpiar?).
- luana ramas `wip/*` (vitalia/nicolify/comunify) **no** tienen el fix del binario (solo main). Propagarlo cuando se toquen.
- luana-platform **bloquea commits directos a main** (pre-commit hook). Para commitear ahí: rama `wip/*`+squash, o `LUANA_ALLOW_MAIN_COMMIT=1 git commit` (NUNCA `--no-verify`).

## Qué está construido y verificado
- **Modelo (cockpit):** unificado en `a39c429`. `portfolioEmpresa` = `{slug, kind, active, sistemas[]}`; cada sistema `{slug, procedencia, key?, ref?, navegable}`. Muestra TODAS las empresas (inactivas con badge "sin cockpit"); sistema sin datos = gap visible (chip "○ slug · procedencia"). `/api/portfolio` (cockpit-go/portfolio.go) + `/directorio` (DirectorioView.tsx) ya consumen esto. **Tests verdes:** go test cockpit-go, vitest 158/2-skip, tsc.
- **chris-corp** (capa DUEÑO, en GitHub): 4 dominios `finanzas/ portfolio/ legal/ harnesses/`; kit adoptado `harnesses/core-harness/`; Odoo scaffold `finanzas/odoo/` (docker Community 18, **sin levantar**); SSoT `portfolio/registry.yaml` → `harnesses/scripts/gen_cockpit_registry.py` → `~/.cockpit/cockpit.yaml` (gated, pre-commit). `core.hooksPath = harnesses/scripts/hooks`.
- **prenter** (EMPRESA, en GitHub): shell de empresa, `kind=factory`. Dominios SEMILLA `sistemas/ empresa/ harnesses/` (NO los de chris-corp). Kit adoptado `harnesses/core-harness/`. **Tiene GATE PROPIO** (`harnesses/scripts/check.py` vía `core.hooksPath`, gate liviano de dogfood — NO corre `/harness:bootstrap`, la máquina SDD de 10 estados es mal fit para un shell de empresa; valida repo-map/registry/ports/ledger + invariante I-39). Seam `project.config.yaml` se conversa en la "visión de Prenter" (diferida).
- **Registry vigente** (`~/.cockpit/cockpit.yaml`, generado por chris-corp, 6 proyectos / 3 activos): prenter(factory,inactive,sistemas=[alpaca-harness **gap**, odoo compartido]), perusaas(own,inactive, **pero su repo SÍ tiene workspace navegable `product`** → aparece navegable en el portfolio), vitalia/nicolify/comunify(own,active,navegables), demo(extra, `kind=""` → excluido del portfolio). Odoo compartido consumido por las 5 empresas.

## El desacople que Fase 1 resolvió ✅ (I-40, commit `c602d75`)
- **Causa raíz (más profunda que "es la UI"):** el SELECTOR se alimenta del `BrandProvider` (SSoT de navegación de las ~20 vistas), y ese provider leía `/api/brands` = `getSelectableBrands` → `multiProjects` = **solo proyectos ACTIVOS**, congelado al arrancar el binario. `/api/portfolio` (lo de `/directorio`) lee `loadRegistry()` = **todo el registry**, por-request. **Dos cómputos distintos de "qué existe"** → prenter/perusaas (`active:false`) nunca entraban; y ya divergían en vivo (`perusaas/product` ES navegable pero `/api/brands` active-only lo omitía).
- **Solución (fuente única, no "que el selector lea portfolio"):** el `BrandProvider` consume `/api/portfolio` y deriva (a) las keys navegables planas (contrato hacia las vistas, **byte-idéntico** al previo) + (b) el árbol para el selector. `/api/brands` queda **CLI-only** (lo usa `cockpit list`; sigue vivo, el daemon lo usa de health-check).
- **Construido (alpaca `c602d75`):** Go `default_brand` en `/api/portfolio` (helper `defaultBrandFromEnv` compartido); `lib/portfolio.ts` helpers de árbol (`navigableKeys`/`sistemasForEmpresa`/`firstNavegableKey`/`empresaHasNavegable` + tests); `BrandProvider` un fetch, expone `tree`+`empresa`/`setEmpresa`; `BrandSwitcher` 2-niveles desde el árbol (inactivas "· sin cockpit"; gaps `<option disabled>`); `AppShell` placeholder + link a `/directorio` para empresa sin navegable (prenter); `EmpresaDependencias` cuelga del árbol del contexto.
- **Verificado en vivo `:4000`:** 5 empresas en el dropdown; `perusaas/product` reaparece (navegable); prenter→placeholder; vitalia→board; `/directorio` consistente. Gates verdes (go test · vitest 162 · tsc · `gen_all --check` sin drift, 40 fichas). Screenshots en el scratchpad de la sesión.

## Lo que Fase 2 agregó ✅ (I-41, alpaca `a1b01b9` + chris-corp `5374012`)
- **Gaps elegibles + overview de sistema.** Los sistemas no-navegables (Odoo compartido, externos, terciarizados, propios sin instrumentar) pasaron de `<option disabled>` a **elegibles** en el dropdown Sistema; al elegirlos, el shell pinta `<SystemOverview>` (procedencia · dueño · ref scopeado por company · descripción · "sin telemetría aún") en vez de romper las vistas brand-scoped.
- **Modelo de estado (clave para Fase 3):** el `BrandProvider` ahora expone `sistema`/`setSistema` (slug del sistema elegido, **puede ser gap**) además de `brand`/`empresa`. **`brand` queda SIEMPRE una key navegable válida** — elegir un gap cambia `sistema` SIN tocar `brand` → `resolveBrand`/`brandPath`/`FileWatch`/story-paths intactos. La intercepción es **única**, en `ShellInner` (`sistemaBySlug(tree, empresa, sistema)` → si `!navegable` → overview); `/harness` (transversal) se excluye. NO se tocó ninguna de las 5 vistas brand-scoped.
- **Componentes:** nuevo `components/layout/SystemOverview.tsx` (hereda el patrón de `NotApplicableForPlatform`); `lib/portfolio.ts` ganó `sistemaBySlug` + `defaultSistemaSlug` (puras, testeadas). El viejo `NoSystemPlaceholder` de empresa se **eliminó** (subsumido: prenter arranca en su primer gap `alpaca-harness` → overview real).
- **Datos:** se rescató el campo `descripcion` (estaba en el SSoT, el generador lo descartaba). Pipeline: `chris-corp/gen_cockpit_registry.py` → `~/.cockpit/cockpit.yaml` → `sistemaMeta`/`portfolioSistema` Go → `PortfolioSistema` TS. **Cero endpoints nuevos** (`/api/portfolio` sigue fuente única).
- **`/directorio` ELIMINADA** (decisión del operador: "no servía, se creó por error" → revierte el Increment 3 de I-36). Borrados `app/directorio/`, `components/directorio/`, item del Sidebar. La navegación por empresa = el selector + la overview.
- **Diferido a Fase 4:** el **semáforo de digitalización** (manual/externo/integrado del mockup Prospera) por sistema — F2 fue plomería estructural, F4 es semántica de negocio.
- **Verificado en vivo `:4000`** (chrome-devtools, opciones nativas leídas por `evaluate_script` sobre `#sistema-switcher`): odoo elegible bajo vitalia → overview scopeada (`company=Vitalia`, dueño "Odoo multi-company (holding)"); prenter → overview de `alpaca-harness` con descripción; vuelta a navegable → Board OK; `/directorio` → 404. Gates verdes (go test · vitest 164 · tsc · `gen_all --check` sin drift, 41 fichas · gate chris-corp).

## Lo que Fase 3 agregó ✅ (I-42 · alpaca `6539479` + chris-corp `ee92192` + PeruSaaS `009bdef`)
- **Sistemas con workspace externo (cross-repo navegable) — el corazón de Fase 3.** Un `sistema` del registry puede declarar `workspace` (un repo/path distinto al de su empresa). El cockpit lo hace **navegable de verdad**: la key sigue siendo `empresa/sistema` (contrato UI intacto, bidireccional) y `resolveSistema` la reenvía vía la tabla **`systemWorkspaces`** (armada de TODOS los proyectos en `serve()`, incl. inactivos) → carga el board del repo externo. NO es una key falsa (resuelve a un `docs/product` que existe; lección anti-I-41-rechazado).
- **prenter → alpaca-harness (flagship):** `registry.yaml` declara `alpaca-harness` con `workspace: ~/Proyectos/prenter-harness` (auto-discover del sistema interno `products`). En vivo: `prenter/alpaca-harness` → **board** (no overview), resuelve cross-repo. **Board fino** (la fábrica no corre SDD sobre sí misma; su evolución vive en el Ledger → idea I-44 para Fase 4).
- **perusaas instrumentado:** `cockpit.active: true` (antes false) → `product/` navegable Y resolvible. Sembradas **2 stories** reales (estado idea) en `PeruSaaS/product/docs/product/stories/` (portal scaffold + provisioning API). Board real verificado en vivo.
- **Bug latente cerrado:** la navegabilidad de un sistema **descubierto** ahora se gatea en `active` (`portfolio.go`). Antes un descubierto bajo empresa inactiva aparecía clicable pero `resolveSistema` fallaba (era el caso perusaas). Ahora: gap honesto hasta activar.
- **Mecanismo (Go):** `sistemaMeta.Workspace` (aditivo/omitempty) · `enableSystemWorkspaces` + `systemWorkspaces` + override en `resolveSistema` + `boardDirFor`/`soleSistemaIn` (workspace.go) · `portfolio.go` emite navegable+key si el board existe · `main.go` arma la tabla en multi. Generador chris-corp: passthrough de `workspace`. 2 tests Go nuevos (workspace externo + active-gating).
- **NO en Fase 3 (a su propia sesión):** chris-corp/**Odoo developable** (instalar starter `odoo-v19` + dev-env + board de addons + nodo chris-corp). El starter odoo-v19 instala el harness de dev pero **NO crea `docs/product`** (hay que sembrar el board). Ver MASTER-PLAN.
- **Verificado en vivo `:4000`** (chrome-devtools): `prenter/alpaca-harness`→board cross-repo; `perusaas/product`→board con 2 stories en "idea" (screenshot); odoo sigue gap/overview bajo cada empresa. Gates: go test (incl. 2 nuevos) · vitest 164 · tsc · `gen_all --check` (44 fichas, 0 avisos) · gate chris-corp.

## Lo que Fase 4 agregó ✅ (I-45 · alpaca `b39b373` + luana `281242df`)
- **Vista "Evolución" — la lente Ledger para sistemas no-SDD (I-45, primer slice de Fase 4).** El board SDD del factory sale vacío (no corre la máquina de 10 estados sobre sí mismo); su evolución vive en el **Ledger** (fichas I-NN §3 + log §5 de PRODUCT-VISION) + semver. Nueva vista **sistema-scoped** "Evolución" (junto a Roadmap, que queda **intacto**) que la muestra.
- **Decisión de alcance (3 forks por AskUserQuestion):** (1) **lente Ledger PRIMERO**, NO la Vista de Negocio empresa-level — esa quedó como **Fase 5** (su dato por-empresa no existe aún); (2) **vista nueva** (no aumentar Roadmap); (3) **artefacto generado** (no parsear PRODUCT-VISION en vivo).
- **Mecanismo:** `tooling/scripts/gen_ledger.py` parsea PRODUCT-VISION.md (fichas §3 + log §5 + versión semver vía git tag, fallback VERSION) → emite `products/docs/product/ledger.yaml`, **gated** en `gen_all --check` (patrón `gen_nodos`; registrado en `repo-map.yaml` `paths.generated` + `_paths.py` `LEDGER_DATA`). Go: `GET /api/ledger` (espejo de `handleSystemMap`; lee el `ledger.yaml` del board dir **resuelto** → cross-repo vía workspace externo I-42; sin archivo → `ledger:null` 200 = **empty-state honesto**, no rompe sistemas SDD) + `handlers_ledger_test.go`. UI: `EvolucionView` (versión + timeline de decisiones nuevo→viejo + fichas I-NN filtrables por estado), item `evolucion` en el Sidebar, `viewAppliesTo` platform=false, tipos `Ledger`/`getLedger`.
- **Dato hoy:** solo el factory (`prenter/alpaca-harness`) tiene `ledger.yaml` (45 fichas · 33 decisiones · v0.4.0). El resto = empty-state ("trackea por Roadmap, no por ledger"). **Dogfood:** la propia ficha **I-45** aparece en el tope de la timeline.
- **⚠ El nav del cockpit es un allowlist por workspace** (`luana-platform/cockpit.config.yaml` `nav:`): un tab NUEVO no aparece hasta agregar su `id` ahí (por eso el commit a luana). El default (sin config) sí trae todos.
- **Verificado en vivo `:4000`** (chrome-devtools, `evaluate_script`): sidebar con "Evolución" tras Roadmap; `prenter/alpaca-harness` → "Ledger de la fábrica · v0.4.0" + 45 fichas + 33 decisiones + I-45 en el tope (screenshot); vitalia → `/api/ledger` = null (empty-state, probado a nivel API). Gates verdes (gen_all --check 45 fichas · vitest 164 · tsc · go test handleLedger).

## Lo que Fase 5 slice 1 agregó ✅ (I-46 · prenter-harness `b164475` + chris-corp `fd4565d` + prenter `71a0e05`)
- **Vista de Negocio empresa-level — la ALTITUD nueva.** Hasta hoy toda vista cuelga de un `sistema` (`?sistema=`); ésta cuelga de la **empresa** (`?empresa=`). Es una **vista transversal empresa-scoped**: va en `CORE_NAV_ITEMS` (como `/harness`, ícono `Building2`) y se **excluye del gap-intercept** de `ShellInner` (`pathname /negocio`), pero lee la `empresa` activa del provider (`useSistema().empresa`). NO toca `CockpitView`/`PLATFORM_VIEWS` (eso es para atenuar sistema-scoped). Componentes: `components/negocio/NegocioView.tsx` + `app/negocio/page.tsx` + tipos `Negocio`/`NegocioObjetivo`/`NegocioArea`/`NegocioProceso`/`Digital` (`lib/types.ts`) + `getNegocio()` (`lib/api-client.ts`).
- **Slice 1 renderiza:** objetivos del directorio (KR from→to) + mapa vivo de áreas→procesos con **semáforo de digitalización** (manual=rojo/externo=ámbar/integrado=verde, diferido de I-41; compone con `procedencia`). **Slice 2 (diferido):** Hilo de oro interactivo + tabla de Brechas (§15).
- **Dato:** curado a mano (salida M1, NO generado) en el **shell** de la empresa: `<repo>/empresa/negocio.yaml`. Sembrado solo **Prenter** (`~/Proyectos/prenter/empresa/negocio.yaml`: 4 objetivos + 7 áreas; `obj[]` por proceso ya listo para el Hilo de Oro; `brechas[]` aún NO — para slice 2). `fuente`/`conf` por proceso (provenance M1).
- **Backend (Go):** `registry.Repo` aditivo (omitempty) — el generador de chris-corp ahora **proyecta `repo`** (el shell) a cockpit.yaml. `GET /api/negocio?empresa=` (`handlers_negocio.go`) valida la empresa contra `multiProjects`, lee `<Repo|Path>/empresa/negocio.yaml`, sin archivo → `negocio:null` 200. Es **I-42 una altitud arriba** (cross-repo a nivel empresa). Test `handlers_negocio_test.go` (shell con/sin archivo · fallback Path · empresa desconocida → 400).
- **Cross-repo probado GRATIS:** `?empresa=vitalia` → `negocio:null` desde `~/Proyectos/vitalia/empresa/negocio.yaml` (su SHELL, no el worktree `luana-vitalia`) — el `repo` passthrough funciona aunque vitalia no esté sembrada.
- **Reconcilió el rename:** slug `alpaca-harness`→`prenter-harness` + workspace vivo en el registry de chris-corp (el rebrand lo había dejado en path muerto). `prenter/prenter-harness` vuelve a cargar board + Evolución.
- **Verificado en vivo `:4000`** (chrome-devtools): `prenter` → diagnóstico real (header + 4 objetivos + 7 áreas + semáforo: Ventas·CRM=manual, Render=externo, Finanzas=externo "Odoo sin levantar"); `vitalia` → null desde su shell. Gates verdes (gen_all `46 fichas` + go test `handleNegocio` · vitest 164 · tsc · gate chris-corp · gate prenter).

## Lo que Fase 5 slice 2 agregó ✅ (I-47 · prenter-harness `11e83f1` + prenter `df57c68`)
- **El Hilo de Oro hecho INTERACTIVO + tabla de Brechas — la Vista de Negocio queda COMPLETA.** Jalar un objetivo del directorio (estado `activeObj` en `NegocioView`) tensa el Hilo de Oro (§19) y gobierna **3 sub-tabs** (Mapa vivo · Hilo de oro · Brechas), sobre el primitivo reusable `Tabs` (variant `line`):
  - **Mapa vivo**: el grid de slice 1, ahora **atenúa** (opacity) los procesos/áreas que NO sostienen el objetivo activo y resalta (borde verde) los que sí. Cada proceso es clicable → drawer.
  - **Hilo de oro** (cascada): lista los procesos sostenedores con un **tag de SALUD** derivado del semáforo (`sostiene`=integrado verde / `depende de tercero`=externo ámbar / `se rompe·manual`=manual rojo) + caja de **huérfanos** (procesos con `obj:[]`).
  - **Brechas** (§15): tabla atada a objetivo con su caso de negocio (tipo · objetivo que bloquea · costo/ref · prioridad), que **resalta** las que bloquean el objetivo activo y atenúa el resto. Hilo coherente de punta a punta: objetivo↔procesos↔brechas reaccionan al MISMO gesto.
- **Drawer** de drill-down por proceso (primitivo reusable `Drawer`, width 440): área · responsable · etapa del ciclo · semáforo · sistema actual · objetivos que sirve (chips) + bloque **Procedencia · sin falsa certeza** (`fuente`+`conf` con dot de color, §14).
- **CERO backend.** `handleNegocio` ya devolvía el YAML entero (I-46) → el `brechas[]` y el `ciclo` del seed fluyen solos al front. Slice 2 = **UI + seed**. UI: `NegocioView.tsx` reescrito; `lib/types.ts` ganó `NegocioBrecha`/`Prioridad` + `ciclo?` en `NegocioProceso` + `brechas?` en `Negocio`. **No se tocó Go ni chris-corp ni luana.**
- **Seed (`prenter/empresa/negocio.yaml`):** `brechas[]` curadas a mano = gaps REALES de la fábrica (CRM en YAML a mano · entrega no probada end-to-end · Odoo sin levantar · discovery artesanal · sin demo guiada · render atado a MCP externo), cada una atada a objetivo + prioridad. `ciclo` por proceso. **Huérfano honesto**: nueva área `Gobierno·Operación interna` con 1 proceso (memoria/orientación del operador) que NO sube a ningún KR — overhead legítimo, ejercita la caja de huérfanos sin inflar.
- **Decisiones del operador (AskUserQuestion):** (1) **3 sub-tabs** fiel al mockup Prospera (no la variante in-place sin tabs); (2) **drawer en slice 2** (no diferido); (3) **Brechas reactivas** al objetivo (resaltar las que bloquean). Realiza **I-12** (Hilo de Oro como vista navegable de primera clase, objetivo↔gap bidireccional).
- **Verificado en vivo `:4000`** (chrome-devtools, localStorage `cockpit:sistema='prenter/prenter-harness'` + ignoreCache): objetivo `pipeline` jalado → mapa atenúa no-sostenedores + cascada con 5 sostenedores tagueados (cerebro/piezas `sostiene`, render `depende de tercero`, CRM/discovery `se rompe·manual`) + huérfano `memoria` + 3 brechas de pipeline resaltadas; drawer de CRM con `Venta`/`Manual`/objetivos/`repo`·`alta`; `vitalia` → `negocio:null` (empty-state cross-repo intacto). Gates verdes (tsc · vitest 164 · `gen_all --check` **47 fichas** + go test · gate prenter). **Dogfood:** I-47 encabeza la timeline de Evolución del factory. Screenshots en el scratchpad de la sesión.
- ⚠ **Sigue vigente:** el tab "Negocio" NO se muestra en `:4000` (luana `nav:` sin `negocio`, intencional) — se verifica navegando directo a `/negocio`. El bug latente platform-pseudo (`getSistemas` vs `getSelectableSistemas`) NO se tocó (la Vista de Negocio lo esquiva validando `empresa`).

## Toolchain (gotchas IMPORTANTES)
- **Go:** NO en PATH. `export PATH="$HOME/.local/go/bin:$PATH"` (go 1.23.4).
- **pnpm:** NO en PATH (`build-ui.sh` lo asume y FALLA). Build de la UI a mano:
  ```bash
  cd ~/Proyectos/prenter-harness/products/cockpit-ui
  mv app/api .api-stash && rm -rf .next out
  COCKPIT_STATIC=1 ./node_modules/.bin/next build      # next local
  rm -rf app/api; mv .api-stash app/api                # restaurar SIEMPRE (trap)
  rm -rf ../cockpit-go/ui && cp -r out ../cockpit-go/ui # embeber
  cd ../cockpit-go && go build -ldflags="-s -w" -o cockpit .
  ```
- **Levantar el cockpit multi (vista de directorio :4000):**
  ```bash
  cd ~/Proyectos/luana-platform
  COCKPIT_MULTI=1 bash scripts/cockpit-daemon.sh start   # stop para bajar
  ```
  Abrir `http://localhost:4000/directorio`. Verificar visual con MCP chrome-devtools (new_page + take_screenshot). ⚠ El binario `cockpit-go/cockpit` puede estar **stale** (no trackeado) — rebuild antes de smoke para ver el modelo nuevo + prenter re-mapeado.
- **Gates (correr antes de commitear):**
  ```bash
  export PATH="$HOME/.local/go/bin:$PATH"
  cd ~/Proyectos/prenter-harness && python3 tooling/scripts/gen_all.py --check        # gate alpaca (incl. go test cockpit-go); corre en su pre-commit hook
  cd ~/Proyectos/chris-corp && python3 harnesses/scripts/gen_cockpit_registry.py --check  # gate registry; corre en su pre-commit hook
  cd ~/Proyectos/prenter-harness/products/cockpit-ui && npx vitest run && npx tsc --noEmit  # UI (npx, no pnpm)
  ```
- **gh:** autenticado como `alpacapurpura` (keyring). `gh repo create` ok.
- **Repo nuevo:** usar `git init -b main` (el default acá es `master`).

## PENDIENTES CONSOLIDADOS (agrupados por cwd — una conversación = un dominio)

> El prompt maestro para retomarlos está en `NEXT-PROMPT.md`. Cada uno = su micro-ciclo.

**A · FÁBRICA (`cd ~/Proyectos/prenter-harness`) — ✅ CERRADO (2026-06-28, ledger I-50, commit `9560154` pusheado)**
- **Deep-link por URL — HECHO.** La navegación viaja como **DOS params** `?empresa=<slug>&sistema=<slug>` (el operador eligió dos vs el `?sistema=` compuesto del plan original, por AskUserQuestion: URL = SSoT total — round-trips board navegable, overview de gap como `?sistema=odoo`, Vista de Negocio empresa-scoped vía `?empresa=` sola, futuras empresas solo-gap). URL gana sobre localStorage al cargar; selector reescribe con `router.replace` (sync bidireccional) + escribe localStorage. **Implementación:** `lib/portfolio.ts` (helpers puros `parseSistemaSearch`/`buildSistemaSearch`/`resolveSistemaIntent`, defensivos) + `SistemaProvider` (lee `window.location.search` —NO `useSearchParams`, que en output:'export' exigiría boundary `<Suspense>`— one-shot al cargar el árbol; write-effect `router.replace`; guard `applyingIntentRef` para no pisar selección de gap). Cero Go, cero otros repos. **Verificado 5/5 `:4000`** por smoke CDP headless (el MCP chrome-devtools cayó por matar su server — recuperado sin deps; ver JOURNAL). Próximo id libre = **I-51**.

**B · chris-corp (`cd ~/Proyectos/chris-corp`) — ✅ CERRADO (2026-06-28 · I-48 ejecutada · 4 commits `db9776e`→`978c0c3`, pusheados)**
- **Home base construida:** el `CLAUDE.md` de chris-corp ya tenía scaffold → se ACTUALIZÓ (no se creó) con § Home base + § Cockpit. Skill **`/orientacion-portfolio`** creado (`.claude/skills/`, gemelo holding de `orientar()`: lee registry→cockpit.yaml + `negocio.yaml` cross-repo + ledgers F/H/L + estado del cockpit). Mecánica interna → **H-03** en `harnesses/DECISIONS.md` (NO consumió I-NN de fábrica; I-51 sigue libre).
- **Launcher rematado** (decisiones del operador por AskUserQuestion): **multi-only** (poda: `cockpit-up.sh` + branch single + detección per-worktree + gate `COCKPIT_MULTI`), **Makefile** (`make cockpit-up/down/status/restart` + `registry`), branding **Prenter Cockpit** (env `COCKPIT_BIN`). El rule colgante `cockpit-alpaca-boundary.md` (vivía en luana, no acá) → doctrina breve en `CLAUDE.md § Cockpit`, ref muerta quitada del daemon. Verificado en vivo: `make cockpit-up`→:4000 HTTP 200, `.cockpit/` gitignored en chris-corp, nav-config `exists:false`→default (tab Negocio se muestra).
- **De-stale alpaca→prenter** (scope descubierto, sweep aprobado): el rebrand quedó a medias en chris-corp → corregido en README/VERSION/finanzas/legal/schema/generador/portfolio (rename + I-39); títulos de ledger históricos (H-01/F-01) y comentarios "antes…/rebrandeado" conservados.

**C · tab "Negocio" / nav — RE-DIAGNOSTICADO en el cierre de A (probablemente ya NO vive en luana)**
- **Hallazgo VERIFICADO (código, no handoff):** el `nav:` allowlist en multi-mode ERA **GLOBAL y cwd-dependiente**. `handleNavConfig` lee `getWorkspaceRoot()+"/cockpit.config.yaml"`; en multi el daemon lanza `env -u WORKSPACE_ROOT` → `getWorkspaceRoot()` cae a `git rev-parse --show-toplevel` desde el **cwd de arranque del daemon**. Con el launcher en chris-corp → su git-root no tiene config → `nav:null` → default completo → tab Negocio se muestra. (Eso seguía siendo así para el caso sin `?sistema=`.)
- **C-resto-(a) ✅ CERRADO (2026-06-28 · ledger I-51 · prenter-harness `4ee76db`, pusheado):** el nav-allowlist multi pasa de global-cwd-dependiente a **per-board**. El front pasa `?sistema=<key navegable>`; el Go `resolveSistema` la traduce a la raíz del repo de ESE board (cross-repo vía `systemWorkspaces`) y lee su `cockpit.config.yaml`. Cada board cura su nav sistema-scoped; las transversales (Negocio/Harness) quedan **always-on** en multi; single-mode intacto (`getWorkspaceRoot`); key que no resuelve → default 200. Solo prenter-harness (`handlers_navconfig.go`+test · `api-client.getNavConfig(sistema)` · `Sidebar` re-fetch `[sistema]` + helper puro `resolveSidebarNav` en `lib/nav.ts`+test · `AppShell` pasa `mode`). Confirmado por AskUserQuestion (per-board · transversales always-on). Verificado `:4000` API (curl: factory→root `prenter-harness`, vitalia→`luana-vitalia/vitalia`, perusaas→`PeruSaaS`, desconocida→default) + **CDP headless 4/4** (factory con `nav:[board,evolucion]`→curado + transversales always-on, vitalia→default aislado, baseline+revert). Mecanismo: sin config per-board presente = default igual (estado hoy).
- **C-resto-(b) ✅ CERRADO (2026-06-28 · luana main `ab32aa01`, pusheado · sin ledger I-NN: es luana, no fábrica):** prosa stale del launcher alineada con la realidad post-I-48. El operador eligió (AskUserQuestion) **full reconcile + folding TODA la deuda adyacente** (alpaca→prenter + `COCKPIT_BIN` + rename de archivo). 9 archivos, todo grounded contra la interfaz REAL de chris-corp (no contra el handoff-foto). **Hecho:**
  - **Comando** `make cockpit-up` (luana, ya no existe) → `make -C ~/Proyectos/chris-corp cockpit-up` en `CLAUDE.md`, `pm-luana/SKILL.md`, `lifecycle.md` (box ASCII re-alineado + caption + item 1), `vitalia/CLAUDE.md`, `Makefile` (el ejemplo `COCKPIT_MULTI=1 bash…` → `make -C … cockpit-up`).
  - **§Cockpit reconciliado:** de per-worktree/per-brand-port (Paradigma A, tabla 4000-4004) → modelo real **single-multi-:4000 consumido desde chris-corp** (luana = consumidor). Borrado el per-brand port table + "por qué per-worktree" + "cockpits coexisten" (modelo muerto).
  - **alpaca→prenter** (el cockpit se renombró Prenter Cockpit): prosa en CLAUDE.md, project.config.yaml, hook `20-story-yaml-valid.sh`, y la rule. `git mv cockpit-alpaca-boundary.md → cockpit-boundary.md` + refs (CLAUDE.md rule#38, project.config.yaml). El env `ALPACA_COCKPIT_BIN` → `COCKPIT_BIN` (la var REAL que lee el daemon — la prosa vieja estaba directamente equivocada). `DEFAULT_BRAND`/`-workspace`/per-brand-port = código MUERTO en el daemon real → eliminados de la prosa.
  - **HISTÓRICOS conservados** (registro, no actualizado): cap-coherence-audit, harness-backlog HB-55, story cockpit-go-process-coverage, ADR-003. **luana se queda en `brands:` POR DISEÑO (I-52)** — no se tocó el seam (era prosa, no la key).
  - **Verificación** (es prosa → cero rebuild): la interfaz real es la fuente de verdad → `chris-corp/.../cockpit-daemon.sh` usa `COCKPIT_BIN` + `-port` (sin `DEFAULT_BRAND`/`-workspace`); `check_cockpit_version` sigue honrando `cockpit_min_version`; `make -C ~/Proyectos/chris-corp cockpit-status` → **UP :4000**. Commit con `LUANA_ALLOW_MAIN_COMMIT=1` (machinery 73/0). **Sync completo:** main→origin + `sync-from-main.sh` propagó a wip/{vitalia,comunify,nicolify} (todos main-ancestor + pusheados); legacy intacto.
  - ✅ **Residual cerrado (2026-06-28 · luana main `3143f843`):** `harness-architecture-guide.md §8` reescrito en voz de guía (cockpit = Prenter, multi-:4000, consumido desde chris-corp; borrado el ref a `tools/luana-cockpit/` muerto). `ADR-003` → sello **SUPERSEDED (I-48)** de 1 línea sobre la tabla per-worktree; el registro histórico se conserva (un ADR documenta el "por qué" de la época — y NO satura contexto, carga on-demand, no always-on). Sync propagado a las 3 wips.

**D · prenter (`cd ~/Proyectos/prenter`) — ✅ CERRADO (2026-06-28 · ledger H-04 · chris-corp `6b4da38` + prenter `d4678bb`)**
- **`crear-pieza-comercial` unificada.** Re-verificado: NO era "17 líneas de voz legítima" — vitalia/comunify/nicolify byte-idénticas entre sí pero **pre-I-39** (versión vieja, 0 customización); PeruSaaS ya reescrita a un núcleo genérico de ~60 líneas (alguien ya extrajo el motor). La versión de prenter era la **más cargada de cosas prenter-específicas**, no el núcleo limpio.
- **Decisión del operador (AskUserQuestion, 4 rondas):** (1) SSoT = **chris-corp** (skill de marketing del HOLDING), **NO el kit que vendemos** (es marketing, no producto); prenter = un consumidor más. (2) Overlay **vía el SEAM** (I-52, consumer class D). (3) **núcleo amplio** (catálogo de 5 piezas + máquina de render H-12 = compartidos). (4) **bespoke_home = puerto abstracto** en el seam (hexagonal) — cada empresa su modelo de venta. (5) técnicas **embebidas inline** (elimina la dep de `methods/registry.yaml`).
- **Hecho:** núcleo canónico agnóstico de empresa (`chris-corp/.claude/skills/`) + `make sync-skills [ONLY=x]` / `-check` (`sync_skills.py`, targetea `empresas[].repo` = los SHELLS, nunca los worktrees luana) + H-04 (`harnesses/DECISIONS.md`). prenter reconciliado: bloque `marketing:` en su seam (design system PRENTER + acento teal + `bespoke_home: clientes/<x>/deals/<y>/comercial/` — el `deals/` correcto de I-39) + SKILL.md sincronizado + de-stale de `marketing/CLAUDE.md`+`README.md`. Gates verdes (check.py prenter · gate chris-corp).
- ⚠ **Cada empresa maneja su PROPIO design system** — el núcleo jamás usa el de otra; si el seam no trae `marketing.*` = gap honesto.
- **Deuda abierta (rollout, cada una su micro-ciclo):** vitalia · comunify · nicolify · PeruSaaS — `make sync-skills ONLY=<slug>` + su bloque `marketing:` con su propio design system + `bespoke_home` según su modelo de venta (comunify B2C · vitalia/nicolify micro-B2B≈directo · PeruSaaS B2B; **preguntar al operador**). Ver [[marketing-skill-portability-debt]].

**E · Deferidos viejos del campaign (su propia sesión)**
- Fase 5c: seed `negocio.yaml` real de vitalia (sesión shell vitalia) — ejercita cross-repo shell≠worktree con dato real.
- Fase 3b: chris-corp/Odoo developable (starter odoo-v19 + board + nodo).
- ~~Fase 3c: rename kit brand→sistema (I-43, bump MAYOR, client-facing).~~ **✅ CERRADO 2026-06-28 (ledger I-52):** NO-breaking vía compat-shim bidireccional (resultó bump MINOR, no MAYOR — el cockpit ya traducía). Kit completo (core-harness + starters dist/ + installer) + 3 seams hermanos migrados (prenter/chris-corp/PeruSaaS); luana queda en `brands:` por diseño. Preservado el sentido dominio (`brand_audit`) + voz. Ver [[kit-consumo-versionado]].
- Levantar Odoo (RUCs + OSE/PSE SUNAT) · ratificar I-NN PeruSaaS (Enterprise→Community) + prenter `ruc: PENDIENTE` · llenar seam de prenter (`project.config.yaml`) + `/harness:bootstrap`.
