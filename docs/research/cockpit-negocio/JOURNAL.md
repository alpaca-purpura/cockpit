# Journal — aprendizajes acumulados

> Append-only. Cada fase agrega lo que aprendió (decisiones, trampas, cosas que NO hacer). Lo más nuevo abajo.

## Semilla (de la sesión que originó la campaña, 2026-06-26/27)

**Modelo / producto**
- Empresa ≠ Sistema (2 niveles reservados aunque hoy varias empresas = 1 sistema).
- Una sola instancia Odoo (compartida). De-dup del compartido = a nivel rollup del dueño, **NO** ocultándolo de la vista de empresa. La vista de empresa es COMPLETA (hay socios por empresa).
- Un sistema sin datos **se muestra** (es un gap de digitalización) — corregido tras feedback del operador. NO ocultar nodos vacíos.
- 2 lentes: cockpit (sistemas/proceso) · Odoo (finanzas) — cruzan en la capa Dueño.
- Empresa-cockpit ≠ company-Odoo (unidad de negocio vs entidad legal con RUC).

**Trampas técnicas (ya resueltas, no repetir)**
- Bug de ruta del binario en luana: era `alpaca-harness/cockpit-go/cockpit`; el realineo 2026-06-25 lo movió a `products/cockpit-go/cockpit`. Arreglado en `main` (no en `wip/*`).
- El cockpit en multi-mode = UN proceso sirviendo N workspaces vía registry `~/.cockpit/cockpit.yaml`; brand-key compuesta = `empresa/sistema` (`resolveBrand`). NO es multi-tenant; es agregador.
- Regla anti-mismatch: los brands DESCUBIERTOS (`docs/product`) mandan la navegación; el registry solo AGREGA sistemas no-brand. Si una empresa tiene brands descubiertos, sus `sistemas` propio declarados NO se duplican.
- `registryProject` Go: campos `Kind`/`Sistemas` son ADITIVOS (omitempty) → backward-compat con cockpit.yaml viejo. No romper esto.
- El selector usa `/api/brands` (activo+navegable). `/api/portfolio` tiene el modelo completo. **Hoy están desacoplados** → Fase 1 los unifica.

**Decisiones del operador (esta sesión)**
- chris-corp es un SISTEMA (Odoo), repo propio en GitHub, desarrollable desde el cockpit con starter `odoo-v19`.
- Prenter = repo nuevo; alpaca-harness es un sistema de Prenter; su cockpit muestra la evolución de alpaca-harness.
- El cockpit evoluciona a herramienta de NEGOCIO (no solo SDD).

**Disciplina de la casa**
- Spec/confirmar antes de codear (sobre todo cambios al cockpit = 1 binario que todas las empresas + clientes corren).
- Gates verdes antes de commitear. Verificar en vivo (smoke + screenshot), no solo tests.
- Ideas de producto → ficha I-NN en `alpaca-harness/tooling/strategy/PRODUCT-VISION.md`.

## Fase 0 — Baseline: commit + repos (cerrada 2026-06-27)

**Lo que pasó (vs lo que el STATE decía)**
- ⚠ **El STATE estaba desactualizado en su premisa:** decía que el Cockpit I-36 estaba sin commitear. **Ya estaba commiteado** (`379d746`). Los 6 archivos modificados eran un **refactor POSTERIOR a I-36**, no un re-commit. Lección: el STATE es snapshot; verificar `git log`/`git diff` contra la realidad ANTES de creerle el "qué falta".
- Ese refactor (commiteado como `a39c429`) **unifica el modelo de portfolio**: `portfolioEmpresa.navegables[]`+`dependencias[]` → un solo `sistemas[]` con `{procedencia, navegable, key}`; muestra TODAS las empresas (incl. inactivas, badge "sin cockpit"); sistema sin datos = chip punteado "○ slug · procedencia" (gap). **Esto es down-payment de Fase 1/2** — el modelo ya hace "todas las empresas + gaps visibles". Lo que falta en Fase 1 es el **selector** (BrandSwitcher usa `/api/brands` = activo+navegable; el modelo completo vive en `/api/portfolio`).

**Trampas / gobernanza nuevas (no repetir a ciegas)**
- **luana-platform tiene pre-commit hook que BLOQUEA commits directos a `main`.** Protocolo: cambios por ramas `wip/*` + squash-merge; para infra/protocolo directo a main hay escape **sancionado** `LUANA_ALLOW_MAIN_COMMIT=1` (y prohíbe `--no-verify`). El fix del binario se commiteó así (`597ea709`). chris-corp NO tiene esto (su gate solo corre el generador del registry).
- **`git init` crea `master`, no `main`.** prenter salió en `master`; hubo que `branch -m main` + `gh repo edit --default-branch main` + borrar `master` remoto. Para el próximo repo: `git init -b main`.

**Decisiones de modelado (Fase 0)**
- **prenter ≠ chris-corp en dominios.** chris-corp = DUEÑO (finanzas/portfolio/legal/harnesses). prenter = EMPRESA → dominios `sistemas/` (catálogo, alpaca-harness #1) + `empresa/` (identidad/socios/objetivos) + `harnesses/` (kit). La operación de Prenter ya vive DENTRO de alpaca-harness; prenter-repo es un **shell de empresa**. Dominios marcados como SEMILLA (refinar en visión de Prenter).
- **alpaca-harness declarada como sistema (gap) de prenter** en el registry (decisión del operador): prenter.repo→~/Proyectos/prenter; sistema `alpaca-harness` propio no-instrumentado = se ve como gap. Instrumentación real (board/roadmap de la fábrica) = Fase 3.
- **Consistencia resuelta:** `odoo` compartido añadido a los sistemas de prenter (estaba en `consumido_por` del Odoo pero no en sus sistemas). Prenter factura → consume el Odoo del holding como las demás.
- **prenter sin starter** (es empresa multi-sistema, no un producto). Seam `project.config.yaml` día-0 parcial (meta/vertical/locale; resto = visión). `.claude/` se cablea con `/harness:bootstrap`.

**Qué NO se hizo (deliberado)**
- Los 3 repos existentes (alpaca/luana/PeruSaaS) quedaron **commiteados pero sin pushear** (el operador pidió "commitear" esos 3 vs "crear y pushear" los 2 nuevos). Cada uno 1-ahead de su origin/main. Pendiente: decidir si se pushean.
- **No hubo smoke en vivo del cockpit** (Fase 0 = mecánica; gates verdes cubren el modelo: `go test`, vitest 158, tsc, gate registry). El render del `/directorio` con prenter re-mapeado se verifica en Fase 1 (es su tema).
- Dato raro a revisar: remoto de alpaca-harness = `alpacapurpura/penter-harness` (¿typo de "prenter-harness"?).

## Empresa-shells de las SaaS luana (sub-fase post-Fase-0, 2026-06-27)

**Disparador:** al revisar luana, el operador aclaró que `luana-vitalia/nicolify/comunify` son **worktrees** (motor `luana core/` + carpeta de marca), y que cada SaaS es independientemente una EMPRESA. Decidió **construir ya** los shells de empresa (no diferir) para empezar a manejar marketing + negocio por-empresa con Claude.

**Principio fijado (vale para todo el portfolio):** Empresa ≠ Sistema. El SaaS (código, genera ingresos) vive en el **worktree** = SISTEMA. La identidad de la empresa (socios, objetivos, RUC, **marketing propio**) NO vive en el worktree (es rama desechable de un codebase compartido; rompe al 2º sistema). Va en un **empresa-shell** = repo propio.

**Construido:** `~/Proyectos/{vitalia,nicolify,comunify}`, los 3 en GitHub (`alpacapurpura`, private, main). Decisiones del operador:
- **Shell de negocio LIVIANO** (NO kit SDD): la máquina de 10 estados/gates es para construir software, y el software vive en el worktree. El shell es workspace de negocio/marketing. (Distinto de prenter, que sí adoptó el kit por ser la fábrica.)
- **Copiar la skill de marketing ahora.** Hallazgo: `crear-pieza-comercial` (y las skills de marketing) son **factory-only** (`.claude/skills/`), NO están en el kit portable (`core-harness/skills` solo trae `harness-bootstrap`). La skill usa rutas **relativas** (`marketing/source/`, `methods/registry.yaml`, `ledger.yaml`) → portable limpia. Se copió verbatim + su maquinaria (`methods/registry.yaml` MK1-5, `_template/copy.md`). El `source/` se sembró como **stubs** (marca propia ≠ Prenter, a llenar con Claude).

**Dominios del shell SaaS:** `empresa/` · `marketing/` · `sistemas/`. (prenter NO lleva `marketing/`: su marketing ES el de la fábrica.)

**Wiring del registry SIN romper el cockpit:** `repo`→shell, `cockpit.path`→worktree. Clave técnica: el campo `repo` del registry **NO se proyecta** a `cockpit.yaml` (el generador solo usa slug/cockpit.path/active/kind/sistemas) → cambiar `repo` es documental, cero efecto en el cockpit; el board sigue navegable desde el worktree. La evolución "el SISTEMA carga su propio path" (separar shell-repo de system-workspace en el schema) sigue siendo Fase 2/3.

**Cómo se construyó (reproducible):** plantilla con placeholders `@@…@@` en `scratchpad/empresa-template/` (autoría limpia) + maquinaria verbatim por `cp` + generación `cp -r` + `sed`. Deja un generador reusable para empresas futuras → **candidato a promover a `new-empresa.sh` en el installer de la fábrica** (no hecho; ofrecido).

**Deuda conocida:** la skill+maquinaria de marketing son COPIAS (3×) que driftan de la fábrica si esta las mejora. Fix correcto = hacer la skill portable (parte del kit que lee el `source/` de cada repo). Pendiente fábrica.

## Fase 1 — Selector = portfolio completo (cerrada 2026-06-27 · ledger I-40 · alpaca `c602d75`)

**El borrador estaba bien en espíritu pero incompleto.** El `phase-1.md` proponía "que el selector consuma `/api/portfolio`". Investigando el código real ANTES de codear (patrón de la casa) apareció lo que el borrador no veía: **el selector no consume nada directo — consume el `BrandProvider`**, que es la SSoT de navegación de las ~20 vistas brand-scoped. El provider leía `/api/brands`. Si solo cambiaba el `BrandSwitcher` y dejaba al provider en `/api/brands`, quedaban **dos fuentes obligadas a coincidir** (frágil). El operador aprobó por AskUserQuestion el enfoque de **fuente única** (provider→`/api/portfolio`).

**La causa raíz era de FUENTE, no de UI (y el gate del repo no la habría cazado — son tests verdes con drift de diseño).**
- `/api/brands` = `getSelectableBrands()` → `multiProjects`, que `main.go` llena con `activeProjects()` = **solo activos**, congelado al arrancar el binario.
- `/api/portfolio` = `loadRegistry()` = **todo el registry**, releído por-request.
- Dos cómputos distintos de "qué existe". prenter/perusaas (`active:false`) nunca entraban al selector.

**Hallazgo en vivo que validó la tesis (verificar EN VIVO, no fiarse de tests):** `perusaas` está `active:false` PERO su repo (`~/Proyectos/PeruSaaS`) tiene un workspace `product/docs/product` → `/api/portfolio` lo reporta **navegable** (`perusaas/product`), mientras `/api/brands` (active-only) lo **omitía**. Es decir: la doble-fuente ya estaba divergiendo en producción, no era hipotético. El fix lo hizo reaparecer. (Corrige el supuesto del `phase-1.md` de que perusaas podía ser "solo gaps": NO, es navegable.)

**Decisiones de UX cerradas con el operador (AskUserQuestion):** (1) `active` = listar todas + marcar inactivas ("· sin cockpit"), consistente con `/directorio`; (2) empresa sin sistema navegable (prenter) → **placeholder inline + link a `/directorio`** (no navegar a una key inválida) — primer ladrillo de la vista overview de Fase 2; (3) gap en el dropdown Sistema = **`<option disabled>`** visible (espeja los chips de `/directorio`).

**Arquitectura técnica (trampas evitadas):**
- Las rutas `/api/portfolio` y `/api/brands` **solo existen en el binario Go** — NO hay `app/api/portfolio/route.ts`. En dev (Next) dan 404; el provider en dev recibe brands por SSR (`getSelectableBrands` en `layout.tsx`), sin fetch. → El cambio solo toca Go + el provider; **dev no se entera** (`brandsProp` no vacío → no fetchea). single-mode y modo per-brand de luana intactos (verificado: binario single-mode en `:4099` da `mode:single`, key plana).
- El contrato hacia abajo NO cambió: las keys navegables derivadas del árbol (`empresa/sistema`) son **byte-idénticas** a las que daba `/api/brands` para las activas → la navegación de las 20 vistas es la misma; solo se AGREGAN empresas no-navegables a la vista del selector. No-breaking de verdad.
- `EmpresaDependencias` y el placeholder siguen a la **empresa seleccionada** (`empresa`), que puede divergir de `empresaOf(brand)` cuando se elige una empresa sin navegable (la `brand` se queda en una key válida; no se rompe).

**Toolchain (confirmado, sin sorpresas nuevas):** go en `~/.local/go/bin` (el pre-commit hook lo saltó — "go no está en PATH, se saltan tests de cockpit-go"; corrí `go test` a mano, verde). Build UI con el `next` local + trap para restaurar `app/api` SIEMPRE. **El binario embebido estaba stale Y el daemon corría el binario VIEJO en memoria** → no basta rebuild: hay que `cockpit-daemon.sh restart` para que cargue el nuevo (el `start` es idempotente y hace no-op si ya corre).

**Commit:** alpaca `c602d75` (local, **NO pusheado** — a decisión del operador). `/api/brands` se conservó (CLI-only + health-check del daemon).

## Fase 2 — Sistemas no-dev de primera clase (cerrada 2026-06-27 · ledger I-41 · alpaca `a1b01b9` + chris-corp `5374012`, pusheados)

**El borrador tenía DOS trampas en su "enfoque candidato" (mismo patrón que F1: la trampa está en el supuesto, no en el qué).** (1) "endpoint/payload nuevo de system-overview" → innecesario: `/api/portfolio` ya trae todo (procedencia/ref/navegable) y el árbol ya vive en el cliente (`BrandProvider.tree`, regalo de F1); un endpoint nuevo re-introduciría la doble-fuente que F1 mató. (2) "key tipo `empresa/odoo` + que `setBrand` a esa key no rompa las vistas" → pelear contra la arquitectura: los gaps tienen `key:""` a propósito; meter una key falsa en `brand` la filtra a TODO lo que lee `brand` (`resolveBrand`/`brandPath` fallan, `FileWatchProvider` se suscribe a un brand fantasma, el reset-effect del provider la expulsa) = el camino "toca todas las vistas".

**La clave que el borrador no vio: el precedente YA existía.** El cockpit ya sabe representar un contexto no-dev: la pseudo-marca `platform` (board+learnings, NO map/roadmap/drift) vía `viewAppliesTo`+`NotApplicableForPlatform` (intercambia el panel, no fetchea-y-rompe). Analogía: `platform` = medio-ciudadano (algunas vistas); **Odoo = invitado** (ninguna vista de dev, solo su "carnet"). F2 no inventó un concepto — generalizó uno en producción un escalón más. Y el premio es el gemelo del de F1: *un punto de intercepción (el shell), no un guard por vista*.

**Enfoque ejecutado (superó al borrador):**
- **Datos:** cero endpoints. Solo se rescató `descripcion` — estaba en el SSoT (`chris-corp/portfolio/registry.yaml`) pero el generador lo descartaba. Pipeline: `gen_cockpit_registry.py` (1 línea) → `sistemaMeta`/`portfolioSistema` Go → `PortfolioSistema` TS.
- **Estado:** `BrandProvider` gana `sistema`/`setSistema` (slug, puede ser gap). `brand` queda **siempre** una key navegable válida — elegir un gap NO toca `brand` (el efecto `[brand]` no dispara, la selección de gap se preserva). Por eso `resolveBrand`/FileWatch/story-paths quedan **intactos** (clave para no "tocar todas las vistas").
- **Intercepción:** UNA, en `ShellInner` — si el sistema seleccionado es gap → `<SystemOverview>` en vez de los children. No se tocó ninguna de las 5 vistas brand-scoped. `/harness` (transversal) se excluye.
- **Subsume** el viejo placeholder de empresa-sin-sistema: prenter arranca en su primer gap (`alpaca-harness`) → muestra su overview real con descripción, no un genérico. Un caso especial menos.
- **Overview scopeada por company** (`ref: odoo#company=Vitalia`), **dueño** derivado de `servicios_compartidos` (nombre "Odoo multi-company (holding)").

**Decisión grande que llegó como NOTA en AskUserQuestion: retirar `/directorio`.** El operador: "esa vista no sirve, se creó por error". Revierte el Increment 3 de I-36. Encaja: la overview por-empresa + el selector subsumen su único rol útil. Borrados `app/directorio/` + `components/directorio/` + item del Sidebar (`Building2`, `showDirectorio`, `DIRECTORIO_ITEM`). Lección: una nota en una pregunta puede traer una decisión más grande que la pregunta — leerla como instrucción y registrarla (ledger) como reversión, no enterrarla.

**Decisiones por AskUserQuestion (3):** (1) overview con descripción; **semáforo de digitalización** (manual/externo/integrado del mockup Prospera) **diferido a Fase 4** — F2 es plomería estructural (hacer al sistema no-dev de primera clase), F4 es semántica de negocio; (2) overview **por-empresa scopeada** (no global) — coincide con "la vista de empresa es COMPLETA" de I-36; (3) /directorio eliminada.

**Toolchain (sin sorpresas nuevas, confirmado):** `descripcion` que sale de chris-corp (`gen_cockpit_registry.py` → `~/.cockpit/cockpit.yaml`) NO está en este repo → su cambio = commit aparte en chris-corp. `ui/` embebida y el binario `cockpit` están **gitignored** (artefactos, no se commitean); `tsconfig.tsbuildinfo` SÍ está trackeado (desde F1) → se commitea. `git push` de alpaca va al remoto `penter-harness` (typo de "prenter", sin corregir aún). Gates verdes (go test, vitest 164, tsc, gen_all --check 41 fichas, gate chris-corp). Smoke en vivo `:4000` con chrome-devtools: las `<option>` nativas se leyeron por `evaluate_script` sobre `#sistema-switcher` (no se ven en screenshot); odoo elegible→overview scopeada, prenter→overview de alpaca-harness, vuelta a navegable→Board OK, `/directorio`→404.

**Commits:** alpaca `a1b01b9` + chris-corp `5374012`, **pusheados** (flota en sync, el operador quiere sync).

## Fase 3 — Instrumentar los sistemas reales (cerrada 2026-06-27 · ledger I-42 · alpaca `6539479` · chris-corp `ee92192` · PeruSaaS `009bdef`, pusheados)

**El borrador (igual que F1/F2) intuía bien pero la trampa estaba en el supuesto.** El `phase-3.md` proponía "el registry de portfolio gana un nivel empresa→sistemas[]→{path,navegable}, puede requerir tocar resolveBrand". Correcto en espíritu. Investigando el código apareció **el contrato duro que el borrador no veía: la key navegable es BIDIRECCIONAL.** El `SistemaProvider` deriva empresa+sistema DE la key (`empresaOf`/`splitSistemaKey`) — así que la key NO puede prefijarse con el repo externo (`alpaca-harness/products` haría saltar el selector de Empresa a una empresa inexistente). La key DEBE ser `empresa/sistema`, y entonces `resolveSistema` SÍ o SÍ tiene que aprender un reenvío. No había forma de evitar tocar el resolver (a diferencia de F2, donde la clave fue NO tocarlo). La diferencia: aquí el override resuelve a un board REAL (no es la "key falsa" que F2 rechazó).

**Mecanismo elegido (confirmado por AskUserQuestion, superó al borrador):** campo aditivo `sistema.workspace` + tabla de reenvío `systemWorkspaces` (key `empresa/sistema` → root externo + sistema local), consultada por `resolveSistema` ANTES del lookup normal. Armada de TODOS los proyectos en `serve()` (incl. inactivos como prenter). `portfolio.go` emite navegable+key si el board existe en disco. Backward-compat (omitempty). Descartados por el operador: **symlink** (ruta frágil, viola I-39, `os.ReadDir` ni lo descubre) y **cockpit.path=repo-externo** (colapsa empresa=sistema, rompe el modelo).

**Hallazgo: "navegable en el selector" ≠ "el board carga".** Hay DOS lecturas del registry: `multiProjects` (arma `resolveSistema`, **solo activos**, congelado en `serve()`) y `loadRegistry()` (arma `/api/portfolio`, todo, por-request + discovery de filesystem). Un sistema descubierto bajo empresa **inactiva** salía clicable en el selector pero `resolveSistema` fallaba (era perusaas/product, el "bug latente" que F1 celebró sin notar). **Fix:** la navegabilidad de descubiertos se gatea en `active` → gap honesto hasta activar. perusaas pasó a `active:true`.

**El factory sale con board VACÍO — y está bien (decisión Q3).** alpaca-harness tiene `products/docs/product` pero es prosa Diátaxis, **cero stories/caps/releases** — la fábrica NO corre la máquina SDD sobre sí misma (gate liviano por diseño). Su evolución vive en el **Ledger** (fichas I-NN) + tags semver. Se hizo navegable igual (arquitectura probada) con **contenido fino**; la "lente ledger/roadmap para sistemas no-SDD" quedó como **idea I-44 para Fase 4** (no sembrar stories sintéticas en el factory).

**chris-corp/Odoo se SACÓ de Fase 3 (a su propia sesión).** Era la pieza más pesada (instalar starter `odoo-v19` + levantar dev-env + sembrar board de addons + agregar el nodo chris-corp). Hallazgo: el starter odoo-v19 (`dist/`) instala el **harness de dev** (.claude rules/skills/agents + gates + spec-templates) pero **NO crea `docs/product`** → instalar el starter ≠ cockpit-navegable; hay que sembrar el board aparte. Diferido por AskUserQuestion (alcance) para cerrar Fase 3 verificada.

**⚡ DETOUR GRANDE: el operador pidió a mitad de fase "elimina lo de brand, son sistemas".** Rename `brand→sistema` del concepto del cockpit. Medido el radio ANTES de tocar (1.650 occ + wire + env + seam + kit shipped) y presentado como bifurcación. Lecciones:
- **El rename se hizo a `sistema` (español), no `system`** — el código YA usaba `sistema` en la capa portfolio + el SSoT + la UI; unificar en español fue lo consistente.
- **El compilador/tests como red de seguridad:** sed por capas (Go → build+test; TS → tsc+vitest) cazó la colisión real (en el provider, `brand`-key y la `sistema`-slug de F2 colisionaban → se desambiguaron en `sistema` vs `sistemaSlug`).
- **Cuando el alcance descubierto difiere materialmente de la pregunta, RE-preguntar.** La pregunta de alcance asumió un sweep mecánico; el kit/starters resultó ser ~280 archivos **destilados, client-facing, con "brand" mezclado con marca-marketing (brand voice/tokens), no verificables acá, breaking = bump mayor**. Se re-presentó → el operador eligió **cockpit ya (verificado) + kit como migración propia**. El **seam key `brands:` se REVIRTIÓ** (viaja con la migración del kit; el cockpit lo lee como key legacy). NO se shippeó kit medio-renombrado.
- **Limpieza de género ES** (sistema=masculino): "desconocido/activo/inválido", "el sistema", etc. — un test esperaba `/inválida/`, actualizado a `/inválido/`.
- Commits del rename: alpaca `42d448d` · luana `31387af2`. Fichas I-43 (rename) + I-44 (lente ledger).

**Toolchain (sin sorpresas nuevas, confirmado):** go en `~/.local/go/bin` (el pre-commit lo corre si está en PATH). Build UI = next local + trap restaura `app/api`. **Fase 3 fue Go-only** (no tocó TS) → bastó `go build` (la ui/ embebida del rename se reusó) + `cockpit-daemon.sh restart`. Smoke con chrome-devtools (`evaluate_script` para opciones nativas + screenshot del board perusaas). Verificación EN VIVO obligatoria: el board cross-repo + las 2 stories de perusaas solo se confirmaron en `:4000`.

**Commits Fase 3:** alpaca `6539479` (cockpit) + `a14e39f` (ledger I-42/43/44) · chris-corp `ee92192` · PeruSaaS `009bdef`, **pusheados** (flota en sync).

## Fase 4 — Cockpit = herramienta de negocio · primer slice = lente Ledger (cerrada 2026-06-27 · ledger I-45 · alpaca `b39b373` + luana `281242df`, pusheados)

**El gran hallazgo de la investigación: la Fase 4 metía DOS cosas distintas en un saco, con riesgos y madurez de dato opuestos — y el modelo de la grande ya estaba diseñado.** (1) **Lente Ledger (I-44)** = por-SISTEMA, para sistemas no-SDD (la fábrica); dato REAL ya existe (44 fichas I-NN + log §5 + tags + FACTORY-BACKLOG). (2) **Vista de Negocio** (objetivos·procesos·gaps + semáforo) = por-EMPRESA (altitud NUEVA: hoy toda vista es sistema-scoped); dato NO existe para ninguna empresa registrada. Y el modelo de la (2) **ya está dibujado como dato** en el mockup Prospera (`prenter/clientes/prospera/.../mockup-cockpit-prospera.html`): `OBJETIVOS{kr,from,to}` + `AREAS→procesos{sistema, digital:manual|externo|integrado, obj[]=Hilo de Oro, puesto, fuente, conf}` + `BRECHAS{obj, costo, prio}`. **No hay que inventar el modelo de negocio — hay que materializarlo** (es el Hilo de Oro de §19 + las 5 dimensiones de §14).

**Decisión (CTO→CEO + AskUserQuestion 3 forks): PARTIR. Fase 4 = la lente Ledger (real, contenida); la Vista de Negocio = Fase 5 (su propia mini-campaña).** Razón: el ledger-lens es dato-real-ya + bajo riesgo + arregla el board vacío del factory + es el PRIMER lente no-SDD (de-risca la altitud empresa que viene). El operador eligió las 3 recomendaciones: (1) ledger primero · (2) vista nueva "Evolución" (Roadmap intacto) · (3) artefacto generado desde PRODUCT-VISION (gated). Y el lean para Fase 5: dato **por-empresa** en doc nuevo (`docs/negocio/…`), NO en el registry.

**Insight de diseño (el porqué encaja):** el cockpit ya sabe "renderizar un nodo por el artefacto que dice su verdad" a pequeña escala (I-41: `SystemOverview` cuando es gap; `viewAppliesTo`/`NotApplicableForPlatform`). La lente Ledger es la siguiente generalización del MISMO gesto: dejar de asumir "sistema = stories SDD". Analogía CEO: el cockpit era un inspector que solo lee planos (specs); la fábrica se construyó sin planos, evolucionó por bitácora (el Ledger). Enseñarle "leé la bitácora cuando no hay plano" (slice chico, real) de-risca el "zoom out a la manzana entera" (la empresa) que viene en Fase 5.

**Lo que funcionó sin sorpresas:**
- **El generador es el patrón propio de la fábrica aplicado al Ledger** (`gen_nodos`-style): fuente única (PRODUCT-VISION) → artefacto-máquina (`ledger.yaml`) → gate anti-drift. Registrado en `repo-map.yaml` `paths.generated` + `_paths.py` `LEDGER_DATA` (Dependency Inversion). Versión = tag semver (git, guarded; fallback `VERSION`) — única dependencia git nueva, justificada (la "evolución" real es el semver).
- **El endpoint reusa la maquinaria cross-repo de I-42 GRATIS.** `/api/ledger` lee del **board dir resuelto** (`sistemaDocsRoot` → `resolveSistema`), así que el `ledger.yaml` del factory (que vive en `alpaca-harness/products/docs/product/`) se sirve cuando navegás `prenter/alpaca-harness` desde luana. Cero plomería nueva de resolución.
- **Empty-state honesto, no 404:** sin `ledger.yaml` → `ledger:null` 200 → la vista dice "trackea por Roadmap, no por ledger". Sistemas SDD no se rompen ni ensucian.

**La trampa de verificación (no fiarse de tests — confirmada otra vez):** la vista renderizaba perfecto por navegación directa PERO el **item del sidebar no aparecía**. NO era bug de código: el sidebar aplica un **allowlist por workspace** (`cockpit.config.yaml` `nav:` del host luana, vía `applyNavConfig`) que no incluía el id nuevo. Dos sub-trampas más en el smoke: (a) el bundle JS quedó **cacheado** (hard-reload `ignoreCache` necesario tras rebuild); (b) `?sistema=` NO override el `localStorage['cockpit:sistema']` viejo → hubo que set-earlo a mano (`prenter/alpaca-harness`) para abrir en el factory. Lección: un tab nuevo del cockpit = **3 lugares** (NAV_ITEMS + `CockpitView`/`viewAppliesTo` + el `nav:` allowlist de cada workspace que lo use).

**Toolchain (confirmado, sin novedad):** go en `~/.local/go/bin` (el pre-commit lo corre). Build UI = next local + trap restaura `app/api`. Rebuild UI+binario + `cockpit-daemon.sh restart` (binario viejo en memoria). El `/api/nav-config` se lee en **runtime** (no embebido) → editar `cockpit.config.yaml` + recargar basta, sin rebuild. luana bloquea main → `LUANA_ALLOW_MAIN_COMMIT=1`. `ledger.yaml` SÍ se trackea (no está bajo el `ui/` gitignored).

**Commits Fase 4:** alpaca `b39b373` (vista + generador + ficha I-45) · luana `281242df` (nav), **pusheados** (flota en sync).

**Fix post-Fase-4 (alpaca `d1a4584`) — trampa `getSistemas` vs `getSelectableSistemas`:** el operador reportó que Evolución en vitalia/nicolify/comunify daba `"sistema desconocido: <empresa>/platform"` (rojo). Diagnóstico: esas 3 empresas resuelven al **pseudo-sistema `platform`** (su board vive en la raíz del worktree, no en un sub-dir). Hay DOS listas de validación en el Go: `getSistemas()` (solo sub-sistemas REALES via `sistemasIn`) vs `getSelectableSistemas()` (reales **+ platform** via `selectableSistemasIn`). Los handlers de lectura que **funcionan** (stories/learnings/releases) validan con `getSelectableSistemas`; los que **rompen para platform** (system-map/value-stream/caps/operator-input) usan `getSistemas`. Copié la guarda de `handleSystemMap` → heredé su bug latente. Fix = una línea (`getSistemas`→`getSelectableSistemas`) + test de regresión platform-pseudo. **Lección:** para validar un `sistema` en un handler de LECTURA del cockpit, usá `getSelectableSistemas()` (lo que el selector ofrece = I-40 fuente única), NUNCA `getSistemas()` (omite platform). El mismo bug late en system-map/value-stream/caps (pre-existente, no tocado — flaggeado en STATE).

**Trampa de toolchain (mordió en el fix):** el trap que restaura `app/api` tras `next build` usa **ruta relativa**, pero el comando hace `cd ../cockpit-go` para `go build` ANTES del EXIT → el restore corre desde el cwd equivocado y **NO restaura `app/api`** (queda como `.api-stash`, `tsc` falla con "Cannot find module app/api/..."). Pasó y se arregló a mano. Para el próximo: restaurar `app/api` ANTES de `cd` al cockpit-go, o usar rutas absolutas en el trap.

## Fase 5 slice 1 — Vista de Negocio empresa-level (cerrada 2026-06-27 · ledger I-46 · prenter-harness `b164475` + chris-corp `fd4565d` + prenter `71a0e05`, pusheados)

**EL TERRENO SE MOVIÓ A MITAD DE ARRANQUE: el operador renombró el repo `alpaca-harness → prenter-harness` (rebrand `ed8ab81`) mientras yo investigaba.** El `cd ~/Proyectos/alpaca-harness` empezó a fallar ("no existe"). Lección reforzada (la de Fase 0, al máximo): **el HEAD/path del handoff es snapshot — verificá `git log -1` + `pwd` contra la realidad ANTES de creerle.** Lo que el handoff decía (HEAD `b39b373`, pre-vuelo "17 archivos sucios del de-branding") quedó **obsoleto**: (a) los 15-17 "sucios" eran **modificaciones FANTASMA** (mtime tocado, contenido byte-idéntico a HEAD — `git diff` vacío, `git update-index --refresh` los limpió); el de-branding se commiteó entero. (b) El repo ya era `~/Proyectos/prenter-harness`, HEAD `ed8ab81`, 4 commits adelante. (c) El typo del remoto `penter`→`prenter` se corrigió solo en el rebrand. **Regla:** cuando el pre-vuelo describe "trabajo sucio pendiente", lo primero es medir si sigue siendo verdad — a veces ya se resolvió de una forma que cambia todo el plan.

**La herida real del rename: un path muerto en OTRO repo.** El rebrand vivía en prenter-harness, pero el registry del cockpit (SSoT en **chris-corp**, repo aparte) seguía apuntando el sistema-fábrica de Prenter a `workspace: ~/Proyectos/alpaca-harness` (muerto) → `prenter/alpaca-harness` no cargaba board ni Evolución. **Lección: un rename de repo deja esquirlas en los repos HERMANOS que lo referencian por path absoluto** (registry, scripts de luana — éstos sí se habían actualizado a prenter-harness). Reconciliar = parte de cerrar el rename: slug `alpaca-harness`→`prenter-harness` + workspace vivo (decisión del operador por AskUserQuestion, consistente con "rebrand todo").

**El diseño (confirmado por AskUserQuestion, 4 forks): seed=Prenter · dato en el shell · slice 1=objetivos+mapa · slug→prenter-harness.** Hallazgos de la investigación que mejoraron el borrador:
- **La altitud nueva es un TERCER tipo de nav, no un refactor del provider.** El agente de arquitectura confirmó: el provider YA expone `empresa` (derivada de la key vía `empresaOf`). La Vista de Negocio = vista **transversal empresa-scoped**, modelada sobre `/harness` (en `CORE_NAV_ITEMS`, sin `view`, excluida del gap-intercept de `ShellInner` por `pathname`), pero leyendo la `empresa` activa en vez de ser global. **Los "3 lugares" de un tab se reducen a 2** para un transversal (CORE_NAV_ITEMS + el `nav:` allowlist; NO toca `CockpitView`/`PLATFORM_VIEWS`, que son para atenuar sistema-scoped en platform).
- **Generado vs curado: decidido por la NATURALEZA del dato, no por reusar maquinaria.** El `ledger.yaml` (I-45) se generó porque su fuente (PRODUCT-VISION) vivía en código. El diagnóstico de negocio es **salida del Levantamiento M1 — conocimiento de cómo opera la empresa, no derivable de ningún repo** → curado a mano, como el `registry.yaml`. No hubo generador.
- **Dónde vive el dato: forzado por la regla "identidad ≠ worktree".** Para las luana el board (`cockpit.path`) ES el worktree desechable → meter ahí la identidad de negocio rompe el principio. Así que el dato DEBE vivir en el **shell** (`<repo>/empresa/negocio.yaml`, junto a `objetivos.md`/`socios.md` que ya estaban ahí). Es I-42 una altitud arriba: el generador de chris-corp ahora proyecta `repo` (el shell, antes se descartaba) a cockpit.yaml; `handleNegocio` lee de ahí (fallback `Path`).
- **Por qué Prenter como primer seed de-risca el slice:** para Prenter `repo == cockpit.path` (ambos `~/Proyectos/prenter`) → el slice 1 NO necesitó plomería cross-repo (el handler lee del path que ya tiene). El caso cross-repo real (shell≠worktree) lo **valida vitalia GRATIS**: `GET /api/negocio?empresa=vitalia` → `negocio:null` desde `~/Proyectos/vitalia/empresa/negocio.yaml` (su shell, NO su worktree) = el `repo` passthrough funciona aunque vitalia no esté sembrada. Honesto y dogfood: el `negocio.yaml` de Prenter expone los gaps de la PROPIA fábrica (CRM = `registry.yaml` a mano → manual; Odoo sin levantar → externo). Provenance por proceso (`fuente`/`conf`): alta donde sale del repo, baja donde es hipótesis (objetivos del directorio).

**Validación: empresa, NO sistema.** `handleNegocio` valida `empresa` contra `multiProjects` (los nombres de empresa del registry), no contra `getSistemas`/`getSelectableSistemas`. Por eso **esquiva el bug latente platform-pseudo** del pre-vuelo (ese es de validación de `sistema`): la Vista de Negocio no valida sistemas. El bug latente queda **sin tocar** (sigue flaggeado en STATE; no lo barrimos — fuera de scope).

**Trampa nueva: un repo hermano (luana) puede revertir tu cambio de config a mitad de sesión.** Edité `luana-platform/cockpit.config.yaml` para agregar `negocio` al `nav:` allowlist (el tab apareció en el smoke). Después, el archivo **se revirtió** a su estado committeado (sin `negocio`, working tree limpio) — la harness lo marcó como cambio externo intencional. **Decisión: respetar el revert (no re-pelear el archivo), NO commitear luana.** Consecuencia: el tab Negocio NO se muestra en el host `:4000` hasta re-agregar `negocio` al allowlist (la VISTA en sí quedó verificada en vivo — el screenshot la capturó renderizando mientras el config la tenía). El binario default (sin `nav:`) sí trae el tab. Si el próximo dev quiere verlo en luana, agrega la línea a mano.

**Lo que funcionó sin sorpresas:** el patrón de F4 (`handleLedger`) fue plantilla exacta de `handleNegocio` (validar → leer → missing:null 200 → parse YAML→JSON); `parseYAMLMap` reusado; el `Repo` aditivo (omitempty) sin romper backward-comp (como `Kind`/`Sistemas`/`Workspace` antes). Gates verdes en cada pre-commit (gen_all `46 fichas`, go test con el nuevo `handleNegocio`, vitest 164, tsc). El build UI + trap-restore-antes-del-cd (lección de F4) salió limpio. Smoke `:4000` con localStorage `cockpit:sistema='prenter/prenter-harness'` (deriva `empresa=prenter`) + `ignoreCache`.

**Memoria:** el dir de memoria migró con el rename (`-alpaca-harness`→`-prenter-harness`); single source, sin split.

## Fase 5 slice 2 — Hilo de Oro interactivo + Brechas (cerrada 2026-06-27 · ledger I-47 · prenter-harness `11e83f1` + prenter `df57c68`, pusheados)

**El handoff acertó: slice 2 fue UI + seed, cero backend.** El pre-vuelo predijo que `handleNegocio` ya devolvía el YAML entero (parsea YAML→map→JSON) → agregar `brechas[]` y `ciclo` al seed fluye solo al front. Confirmado: no se tocó una línea de Go. La investigación lo verificó ANTES de codear (no a ciegas): leí `handlers_negocio.go` + el shape que `getNegocio` espera. Lección reforzada: cuando el backend es un passthrough genérico, evolucionar el dato = evolucionar el seed + los tipos, no el endpoint.

**El primitivo ya existía (gemelo de la lección de F2/F3).** El borrador mental imaginaba construir tabs/drawer a mano. La investigación encontró `components/ui/Tabs.tsx` (variant `line` = exactamente el borde-inferior del mockup) y `components/ui/Drawer.tsx` (scrim + Esc + slide, ya cableados) y `Card`/`cn`. Reusar > reinventar: el `NegocioView` quedó declarativo (un array de `tabs`, un `<Drawer>`), no un framework de tabs casero. **Buscar el primitivo antes de escribir uno** es parte del método, no un extra.

**La decisión de layout fue del operador, y eligió la MÁS fiel (3 sub-tabs), no la más magra.** Le presenté 3 opciones con previews ASCII (in-place sin tabs / in-place + cascada / 3 sub-tabs) recomendando la in-place por ser fiel al idioma tab-free del cockpit. Eligió **3 sub-tabs** (fidelidad al mockup Prospera) + drawer en este slice + brechas reactivas. Lección: mi recomendación de "magro/idiomático" no siempre gana — cuando hay un mockup aprobado que el operador quiere materializar, la fidelidad pesa. El preview ASCII fue clave para que decidiera con la imagen, no con la prosa.

**El estado compartido vive ARRIBA de los tabs, no en ellos.** El filtro de objetivo (`activeObj`) gobierna las 3 lentes a la vez → tiene que vivir en `NegocioView` (padre), no dentro del `Tabs` (que solo maneja su propio tab activo). Los objetivos clicables van ARRIBA del `<Tabs>` (siempre visibles, como el `obj-strip` del mockup); cada tab recibe `activeObj` por closure. El `Tabs` solo conmuta qué panel se monta — y como solo monta `current.content`, el estado del filtro (en el padre) sobrevive al cambio de tab. Patrón limpio: shared-state-up, view-switch-down.

**El huérfano honesto: cuando el dato real no ejercita la feature, agregá dato REAL que sí — no sintético.** El seed de slice 1 no tenía procesos con `obj:[]` (todo laddera a un objetivo — buena señal para una fábrica magra), así que la caja de huérfanos del Hilo de oro saldría vacía y la feature parecería muerta. En vez de inventar un huérfano falso, agregué uno **real**: la memoria/orientación del operador (overhead de gobierno que genuinamente no sube a ningún KR del directorio), en un área nueva `Gobierno·Operación interna`. Honesto Y demostrativo. Lección: una feature de "lo que falta/sobra" necesita al menos un caso real que la dispare; si no existe, buscá el caso real (siempre lo hay), no uno de cartón.

**Brechas reales = dogfood que pica.** El operador eligió que las brechas fueran los gaps de la PROPIA fábrica: CRM en YAML a mano, entrega no probada end-to-end, Odoo sin levantar, discovery artesanal, sin demo guiada, render atado a un MCP externo. La Vista de Negocio de Prenter es, literalmente, el diagnóstico de Prenter — el producto se prueba diciéndole verdades incómodas a su dueño. (El mismo gesto que el "honestidad sin falsa certeza" del §14: `fuente`+`conf` por proceso, ahora visible en el drawer.)

**Toolchain (sin sorpresas nuevas, todo confirmado):** build UI = next local + **trap que restaura `app/api` ANTES del `cd` a cockpit-go** (lección F4, ruta absoluta en el trap) — salió limpio. Rebuild UI+binario + `cockpit-daemon.sh restart` (binario viejo en memoria). Smoke `:4000` con `localStorage cockpit:sistema='prenter/prenter-harness'` + `ignoreCache`; los clics de tab/objetivo/proceso se dispararon por `evaluate_script` (más robusto que adivinar uids), screenshot por estado. El tab "Negocio" sigue oculto en luana (`nav:` sin `negocio`, intencional) → se navega directo a `/negocio`. Gate prenter-harness regeneró `ledger.yaml` (47 fichas, I-47 en el tope) — se commitea (no está bajo `ui/` gitignored). Gates verdes (tsc · vitest 164 · gen_all --check · gate prenter).

**Commits Fase 5 slice 2:** prenter-harness `11e83f1` (cockpit UI + ficha I-47 + ledger.yaml) + prenter `df57c68` (seed), **pusheados** (flota en sync). **Vista de Negocio COMPLETA — el titular de la campaña, entregado.**

## Limpieza de fábrica — cierre de pendientes chicos (2026-06-27 · 4 commits, pusheados)

Sesión SEPARADA (no una fase de la campaña): cerrar los pendientes CHICOS que quedaron tras Fase 5. Regla: cada item = su micro-ciclo (investigar código real → AskUserQuestion → ejecutar chico → gates → verificar en vivo → **commit independiente**). 5 items: 1,2,3,5 ejecutados · 4 diferido. Commits `57d09c4`·`8b2c9ba`·`1e4f64f`·`edbea40`.

**El bug "latente" lo era de verdad — y la trampa fue creer que rompía navegando (no rompe).** ITEM 1 (platform-pseudo): el handoff decía que system-map/value-stream/capabilities revientan para vitalia/nicolify/comunify. **Confirmé en vivo ANTES de tocar** (curl: 7 endpoints → 400 `"sistema desconocido: <empresa>/platform"`). PERO investigando el código de la UE: `MapView`/`DriftView` **cortan `isPlatform(sistema)` DENTRO de su `load()`** (antes de fetchear) → la vista pinta `NotApplicableForPlatform` y **nunca llama** al endpoint roto. O sea: el bug es de **contrato de API** (alcanzable por curl o futuro caller), NO de navegación actual. Se arregló igual (defensa-en-profundidad + consistencia con `handleLedger`/`handleOperatorInput` ya correctos). Lección: "confirmá el bug en vivo" tiene dos mitades — confirmar que EXISTE (curl) y confirmar CÓMO se alcanza (a veces la UI ya lo blinda; eso cambia el relato, no la corrección).

**Casi colisiono un id de ledger en código.** Tagueé el fix del ITEM 1 como "I-48" en comentarios+commit. Pero I-48 estaba reservado para chris-corp (ITEM 5). Un bugfix NO gana ficha (es derivable — el fix idéntico de F4 viajó bajo I-45). Re-anclé a **I-40** (la ficha que fijó `getSelectableSistemas` = lo que ofrece el selector = lo que valida la lectura) y amendé ANTES de pushear. Lección: no hardcodear un id de ledger sin asignar en código; un bugfix se ancla a la ficha del PRINCIPIO que aplica, no consume id nuevo.

**El terreno se movió OTRA VEZ — esta vez para ITEM 4.** El prompt (y la memoria) asumían que `crear-pieza-comercial` es factory-only, copiada 3× a los shells. Verifiqué contra la realidad: el gate reporta **8 skills, no 9** → I-39 ya **migró el skill a la EMPRESA** (`ports.yaml` lo documenta: `MIGRADO a prenter`). Hoy son **4 copias en repos hermanos, 0 en la fábrica**. → ITEM 4 es **silla equivocada** desde la fábrica (no hay mitad-fábrica honesta; el kit es SDD, meter marketing ahí contradice I-39). Diferido a sesión prenter, memoria corregida. Lección REFORZADA (la de F0/F5 al máximo): el handoff/memoria es foto — antes de "esto vive en la fábrica", `grep` la realidad (el conteo del gate delató el movimiento).

**negocio.schema: el primer contrato cross-repo rompió la fitness function — y eso fue una decisión, no un parche.** ITEM 3: el `negocio.yaml` vive en los shells (otros repos). Al autorear `negocio.schema.yaml` (L0) la fitness function (`validate_architecture`) falló: exige que `aplica_a` resuelva a una ruta factory-local (guarda anti-fantasma). negocio es el PRIMER schema cuyas instancias viven legítimamente afuera (I-39). Fix honesto (no falsear `aplica_a` a una ruta real): convención **`instancias_externas: true`** que la fitness function reconoce y exime — EXPLÍCITA, para no relajar la guarda del resto. Es H-17 (mecanismo de fábrica). Lección: cuando un contrato nuevo choca con el gate, preguntar si el gate necesita aprender un concepto nuevo (cross-repo) antes que doblegar el contrato.

**Dónde validar dato cross-repo: el chokepoint, no cada puerta.** ITEM 3 fork (AskUserQuestion): el `negocio.yaml` se valida (a) en el gate de cada shell, (b) al leer en el cockpit, o (c) ambas. Elegí/recomendé **(b)**: el cockpit es el AGREGADOR — el único punto por el que pasa TODO negocio.yaml de toda empresa → un validador ahí cubre a todas sin distribuir nada a N shells (el shell-gate exigiría cablear cada repo o un pack del kit). Analogía CEO que cerró la decisión: un detector de metales en el único lobby compartido, no uno por oficina.

**Verificar el banner sin poder editar otro repo: perturbación temporal + revert.** El banner de `negocio.yaml` mal formado solo aparece con dato malo, que vive en prenter (otro repo, no se toca). Para el smoke positivo: perturbé `prenter/empresa/negocio.yaml` con sed (digital typo + obj fantasma), recargué, screenshot del banner con los 2 warnings nombrando proceso/área, y **`git checkout` inmediato** (confirmé clean antes y después, HEAD intacto). Es verificación, no trabajo cross-repo (cero commit a prenter). Negativo (seed válido → sin banner + `warnings:[]`) y reload→limpio también verificados.

**chrome-devtools estaba wedge: matar el browser huérfano, NO el server MCP.** El MCP daba `"browser is already running for .../luana-vitalia-H"` y `"Restore window to normal state before setting content size"`. Causa: un Chrome huérfano de sesión previa con estado de ventana maximizada guardado en el profile. Recuperación: `kill` SOLO el árbol del browser (PID con `--user-data-dir=...luana-vitalia-H`), **NUNCA** el server MCP (`npm exec chrome-devtools-mcp`), + `rm -rf` el profile (cache regenerable) para resetear el estado de ventana. Tras eso, `new_page` levantó limpio. (El operador confirmó: usar lane `luana-vitalia-H`.)

**Toolchain (sin sorpresas nuevas):** los 2 rebuilds de UI (items 2 y 3) usaron el trap-restaura-`app/api`-antes-del-`cd` (ruta absoluta, lección F4) — limpios. Para cambios Go-only (ITEM 1) basta `go build` (reusa la `ui/` embebida) + `cockpit-daemon.sh restart`. Cada commit pasó su pre-commit `gen_all --check`; el hook saltó `go test` (go no en su PATH) → lo corrí a mano verde. Smoke con localStorage `cockpit:sistema='prenter/prenter-harness'` + `ignoreCache`; selector de empresa cambiado por `Object.getOwnPropertyDescriptor(...).set` + `dispatchEvent('change')` (React-compatible) — clave para probar la memoria-en-sesión SIN remontar el componente (un reload sí la borra, by design).

**Commits Limpieza:** `57d09c4` (ITEM 1 platform-pseudo) · `8b2c9ba` (ITEM 2 filtro recordado) · `1e4f64f` (ITEM 3 negocio.schema + validación) · `edbea40` (ITEM 5 ledger I-48/I-49/H-17). Pusheados (`11e83f1..edbea40`). Memorias corregidas: [[marketing-skill-portability-debt]] (obsoleta), [[harness-factory-architecture]] (9→8 + 5º schema + H-17), [[portfolio-holding-model]] (I-48).

## Pendiente A — Deep-link por URL (2026-06-28 · ledger I-50 · prenter-harness `9560154`, pusheado)

**El operador cazó un error de handoff ANTES de que lo arrastrara, y la lección es sobre las fuentes.** El prompt (y el encabezado del pendiente C en STATE) decía "C · luana"; lo repetí como "el tab Negocio vive en luana". El operador frenó: "¿por qué luana si el launcher ya está en chris-corp?". Tenía razón doble: el propio STATE marcaba ese punto con ⚠ VERIFICAR, y yo había tomado la etiqueta cwd e ignorado el warning pegado a ella. **Regla reforzada:** una etiqueta de cwd en el handoff es una hipótesis, no un hecho — y si el doc mismo dice "verificar", ESO es la instrucción, no el título. El operador además fijó la política: errores en docs TEMPORALES (campaign/) tolerables; en docs de harness/proyectos, no.

**El hallazgo que salió de verificar C (vale para el futuro de C):** el `nav:` allowlist del cockpit en multi-mode es **GLOBAL y depende del cwd del daemon**, no per-board. Cadena: `handleNavConfig` lee `getWorkspaceRoot()/cockpit.config.yaml`; en multi el daemon lanza `env -u WORKSPACE_ROOT` → `getWorkspaceRoot()` cae a `git rev-parse --show-toplevel` desde el cwd de arranque. Con el launcher en chris-corp, gobierna el git-root del daemon (sin `cockpit.config.yaml` → `nav:null` → default completo → Negocio se muestra), NO luana. Confirmado en vivo (`/api/nav-config` → `exists:false`). → C se auto-resolvió en gran parte; el resto es decisión de fábrica (¿per-board?) + limpieza de prosa en luana.

**Verificar TODO el plan antes de proponer pagó (no asumir nada, a pedido del operador).** Antes de la AskUserQuestion leí el provider entero, los helpers de `portfolio.ts`, el layout, el gap-intercept de `AppShell`, cómo navega el `Sidebar` (Link sin query → el write-effect re-agrega), el `next.config` (output:'export'), y el próximo id de ledger. Ese último casi muerde: `grep I-50` daba I-50 **e I-51** en el archivo → pánico momentáneo → eran solo el comentario placeholder `<!-- Próximas ideas… I-50, I-51 -->`. La última ficha REAL era I-49. Lección: `grep` de un id puede pegar en prosa/placeholder; confirmá que es una ficha, no una mención.

**Decisión de diseño que liberó el modelo:** la forma de la URL del navegador es INDEPENDIENTE de la del API Go. El front arma `?sistema=empresa/sistema` (compuesto) para los `fetch` DESDE el estado, no desde `window.location` → libre de elegir `?empresa=`+`?sistema=` (dos params) en la barra. El operador eligió dos (URL = SSoT total) sobre el `?sistema=` compuesto del plan original.

**Trampa React que el guard resolvió:** el provider deriva `empresa`/`sistemaSlug` de la key `sistema` en un efecto `[sistema]`. Al hidratar un deep-link a un GAP, la key activa es un navegable de FONDO cuyo slug ≠ el gap elegido → ese efecto pisaría la selección. Fix: `applyingIntentRef` salta la derivación UNA vez, solo cuando el hydration movió la key. Patrón: aplicar un triple (key, empresa, slug) coherente de una, con un guard contra el efecto derivador.

**`useSearchParams` evitado a propósito:** en `output:'export'` exige un boundary `<Suspense>` alrededor del componente que lo use — y el provider monta alto (envuelve todo el shell). Leer `window.location.search` directo (el provider ya gatea `typeof window`) lo esquiva. El build confirmó: las 11 rutas generaron sin el error de Suspense.

**LA TRAMPA GRANDE que pagué: maté el server MCP, no el browser.** El chrome-devtools MCP estaba wedge ("Restore window to normal state…"). El journal de la Limpieza advertía: matar SOLO el árbol del browser, NUNCA el server. Usé `pkill -f "user-data-dir=...luana-vitalia-H"` — pero el patrón **se incluía a sí mismo** (mi propia línea de comando contenía el string) → exit 144 a media ejecución, y peor, en el retry maté por PID los procesos `npm exec chrome-devtools-mcp …userDataDir=…/luana-vitalia-H` creyendo que eran browser remanentes — eran el **server** de la lane H. La sesión perdió las tools chrome-devtools (no vuelven). **Lecciones:** (1) `pkill -f <patrón>` te mata a vos si el patrón aparece en tu propia cmdline — filtrá `$$`/`grep -v bash` o matá por PID con cuidado; (2) `npm exec chrome-devtools-mcp` = SERVER (no tocar); el browser es `/opt/google/chrome/chrome --user-data-dir=…<lane>`; (3) ante duda, NO hagas cleanup masivo del MCP — pedí o usá otra lane.

**Recuperación sin el MCP (cero deps, contra el binario real):** Node v24 trae `WebSocket` global → manejé Chrome headless por el DevTools Protocol directo (`scratchpad/cdp-smoke.mjs`): `Target.createTarget`/`attachToTarget` (flatten) → `Page`/`Runtime.enable` → `Page.navigate` + `Runtime.evaluate` para leer `location.search`/valores de selector/innerText. Profile temporal limpio (evita el wedge), `--headless=new --remote-debugging-port`. 5/5 escenarios PASS contra `:4000`. **Quedó un harness de smoke reusable** cuando el MCP no esté: más robusto que el MCP para asserts programáticos (lee estado, no screenshots). Marcadores estables: overview=`"sin telemetría aún"`, Negocio=`"diagnóstico de negocio"`. Cambio de `<select>` React-compatible: native value setter + `dispatchEvent('change')` (mismo truco que la Limpieza usó para localStorage).

**Higiene:** el daemon crea `.cockpit/` (log+pid) en su git-root de arranque; al lanzarlo desde la raíz prenter-harness apareció ahí (untracked, NO trackear) → lo borré tras el commit (su home es chris-corp). El commit fue de 6 archivos explícitos (provider, helpers, test, `tsconfig.tsbuildinfo`, `ledger.yaml` regenerado, PRODUCT-VISION), sin `.cockpit/`.

**Commit A:** prenter-harness `9560154` (`edbea40..9560154`), pusheado. Gates: vitest 173 (+8) · tsc · `gen_all --check` (50 fichas, go test ok) · pre-commit verde. Memoria [[cockpit-business-tool-campaign]] actualizada (A cerrado + hallazgo nav-config para C).

## Pendiente B — chris-corp home base (2026-06-28 · I-48 ejecutada · chris-corp `db9776e`→`978c0c3`, pusheado)

**"Construir el CLAUDE.md" cuando el CLAUDE.md YA existía — la foto otra vez.** El handoff pintaba B como "crear el `CLAUDE.md` de chris-corp + el skill". Verifiqué contra el repo: el `CLAUDE.md` **ya existía** (scaffold 2026-06-26) — era *actualizar* (doctrina home-base + § Cockpit + de-stale), no crear. Solo el skill `orientacion-portfolio` era construcción real. **Regla reforzada (F0/F5/Limpieza al máximo):** un handoff que dice "construí X" igual exige `ls`/`git log` para ver si X ya está y en qué estado — "construir" puede ser "rematar".

**Un script que se muda arrastra referencias a archivos que NO se mudaron con él.** El `cockpit-daemon.sh` (movido de luana a chris-corp) referenciaba `.claude/rules/cockpit-alpaca-boundary.md` y `make cockpit-up` — **ninguno existía en chris-corp** (el rule vivía en luana; no había Makefile). Decisión: NO copiar el rule de luana (sus 5 contenciones son luana-específicas: no-forkear el cockpit + fuga "Marca") — puse la doctrina breve consume-por-versión en `CLAUDE.md § Cockpit` y quité la ref muerta. El Makefile sí se creó (vuelve verdad la prosa). **Lección:** al rematar un script mudado, listá sus refs a archivos y resolvé cada una (existe acá / traer adaptado / reescribir como doctrina inline) — no las dejes colgando.

**El scope se escondía en el de-stale: el rebrand alpaca→prenter nunca aterrizó del todo en chris-corp.** Al de-stale-ar los mapas (regla de oro: actualizar map+repo-map en el mismo commit) `git grep alpaca-harness` delató ~17 refs más — algunas operativamente erróneas (`portfolio/README` mandaba `cd luana && make cockpit-multi`, target ya inexistente; schema/generador decían clientes en `alpaca-harness/clients/`, contra I-39). Eso es "creció más de lo dicho" → **FRENÉ y re-pregunté** (AskUserQuestion), el operador eligió sweep completo. **Lección:** de-stale-ar los archivos que tocás destapa deuda adyacente — preséntala como decisión (hacer/diferir), NO la expandas en silencio ni la dejes en silencio. Y al barrer un rename: distinguí **doc/puntero navegable** (se actualiza) de **título de ledger histórico** (se conserva, es registro de lo decidido entonces).

**`make` grita "Error 1" si el target propaga un exit≠0 legítimo.** `cockpit-status` cuando el cockpit está DOWN devuelve 1 (semántica del daemon, útil para scripting `&&`). Vía `make` eso imprime `*** [Makefile] Error 1` — ruido para uso interactivo. Fix: `@bash $(DAEMON) status || true` en el target (el daemon conserva su exit code si lo llamás directo; make queda limpio). **Lección:** un exit-code-con-significado de un script no siempre quiere propagarse por el Makefile — swallow en la capa make, preservá la semántica en la capa script.

**Tocar un generador GATED: probá que el output no se movió.** Edité comentarios + un string de error en `gen_cockpit_registry.py` (de-stale) — riesgo: cambiar la salida y romper el gate anti-drift. Verifiqué `python3 -c ast.parse(...)` (sintaxis) + `--check` (output byte-idéntico → sin drift) ANTES de commitear. **Lección:** cuando el de-stale toca código gateado, los cambios deben ser provablemente no-funcionales (comentarios/strings de error que no salen al artefacto) y se confirma con el propio gate.

**La poda se verificó muerta antes de cortar.** Antes de borrar el single-mode (cockpit-up.sh + branch del daemon), `grep` confirmó que nadie lo llama (luana borró sus targets `make cockpit-*`; cockpit-up.sh solo se referenciaba a sí mismo) — y es recuperable por git igual. El operador eligió eliminar (no conservar) por AskUserQuestion. **Lección:** poda = confirmar cero-callers + reversibilidad (git) ANTES, no después.

**Decisiones del operador (AskUserQuestion):** (1) **eliminar single** (solo multi); (2) **Makefile** (vs wrapper/solo-doc); (3) branding **Prenter Cockpit** (el binario es producto Prenter); (4) **sweep completo** del de-stale. Mecánica interna del holding → **H-03** en `harnesses/DECISIONS.md` (la decisión de PRODUCTO ya era I-48 en el ledger de prenter-harness; B no consumió I-NN nuevo → I-51 sigue libre).

**Commits B:** `db9776e` (launcher: multi-only + Makefile + branding) · `c885e03` (skill orientacion-portfolio) · `1f45b4f` (doctrina home-base + § Cockpit + H-03 + de-stale mapas) · `978c0c3` (de-stale sweep completo), pusheados (`e38706f..978c0c3`). Gate `gen_cockpit_registry --check` verde en cada uno.

## Pendiente C-resto-(a) — Nav-allowlist per-board en multi (2026-06-28 · ledger I-51 · prenter-harness `4ee76db`, pusheado)

**Por una vez el handoff coincidió con la realidad — y aun así verifiqué todo antes de tocar.** `git log -1` = `9560154` (= STATE), árbol limpio, código tal cual el hallazgo de A. La disciplina no cambia porque la foto esté fresca: confirmé cwd + HEAD + el próximo id del ledger + el código de los 3 puntos (`handleNavConfig`, `getWorkspaceRoot`, `resolveSistema`) antes de la AskUserQuestion. El id casi muerde otra vez igual que en A: `grep I-5` pega I-51 **e I-52** en el placeholder `<!-- Próximas ideas… I-52, I-53 -->`; la última ficha REAL era I-50 → libre = I-51. Lección reforzada: el grep de un id pega en el comentario-placeholder; confirmá que es una ficha `###`, no una mención.

**El hallazgo de diseño que el handoff no veía: hay DOS familias de tabs, y el per-board solo aplica a una.** El `nav:` filtra `NAV_ITEMS` (sistema-scoped) **y** `CORE_NAV_ITEMS` (transversal: Negocio=empresa · Harness=global) con la misma lista. Si hacés per-board sin distinguir, un board podría tachar la pestaña Negocio de SU empresa o el Harness global — y peor, parpadearían al cambiar de board. Por eso el matiz "¿quién gobierna las transversales?" no era cosmético. Resolución (AskUserQuestion): **always-on en multi** (no cuelgan de un board) · single-mode las sigue gobernando (caso cliente). Lección: cuando una feature toca un mecanismo que mezcla dos conceptos (aquí: tabs-de-board vs tabs-transversales), el fork de "¿a cuál de los dos aplica?" es la decisión real — flaggealo desde la opción y confirmalo aparte.

**Per-board lee la raíz del board RESUELTO, no la del cockpit.path de la empresa — y eso desnuda un archivo muerto.** `resolveSistema("vitalia/platform")` → `luana-vitalia/vitalia` (el worktree); la fábrica `prenter/prenter-harness` → `prenter-harness` (cross-repo vía `systemWorkspaces`). El único `cockpit.config.yaml` de la flota está en `luana-platform/` — y **ningún board resuelve ahí** → quedó **muerto en multi** sin importar el cwd del daemon. Refuerza que C-resto-(b) (limpiar prosa en luana) es harina de otro cwd: el archivo de config viejo ya no gobierna nada en multi.

**Seguí la convención de tests: lógica pura a `lib/`, no importar el componente.** Los 18 tests vitest viven todos en `lib/` (ninguno importa un `.tsx`). En vez de exportar `resolveSidebarNav` desde `Sidebar.tsx` (y arrastrar lucide/next/navigation al test), lo extraje a **`lib/nav.ts` genérico sobre `{id}`** (sin acoplar a `NavItem`/lucide) → testeable como los demás. Lección: antes de exportar una función de un componente para testearla, mirá DÓNDE viven los tests del repo y movela ahí; la convención de ubicación es parte del método.

**Verificar un cambio "no-op por default" exige perturbar para VERLO.** El per-board es mecanismo: sin ningún `cockpit.config.yaml` per-board presente (estado hoy), todos los boards muestran el default — la UI en vivo se ve idéntica a antes. Para probar la conducta nueva dropéé un `cockpit.config.yaml` temporal en la raíz de la fábrica (`nav:[board,evolucion]`), verifiqué (factory→curado + transversales always-on · vitalia→default aislado) y **revertí** (rm) — patrón perturbación/revert de la Limpieza (el banner negocio.yaml). El `nav-config` se lee en **runtime** (no embebido) → el drop/rm del archivo se ve sin rebuild; solo el cambio de Go/UI exigió rebuild+restart. Confirmé git limpio tras el rm.

**El daemon se reinicia desde su home base (chris-corp), no se arranca uno nuevo en el cwd de trabajo.** Estaba parado en prenter-harness; el daemon corría en `:4000` desde el smoke de B (home base chris-corp). `COCKPIT_BIN` default apunta al binario de prenter-harness → rebuildié el binario acá y reinicié con `cockpit-daemon.sh restart` **desde chris-corp** (vía `cd $HOME/Proyectos/chris-corp`). Así el `.cockpit/` (pid/log) sigue en chris-corp y `getWorkspaceRoot()` del daemon no migra. Lección: separá "dónde se construye el binario" (prenter-harness) de "desde dónde corre el daemon" (chris-corp home base) — no levantes un daemon en el cwd de trabajo.

**Toolchain (sin sorpresas):** build UI = next local + trap restaura `app/api` con **ruta absoluta ANTES del `cd`** (lección F4) — limpio. Rebuild UI+binario + restart desde chris-corp. MCP chrome-devtools no se tocó (fui directo al CDP headless, más robusto para asserts programáticos); recreé el harness en mi scratchpad (`cdp-nav-smoke.mjs`, lee `aside nav a`). Gates verdes: go test (`handlers_navconfig_test.go`) · vitest **180** (+7 `lib/__tests__/nav.test.ts`) · tsc · `gen_all --check` (**51 fichas**, ledger.yaml regenerado). Pre-commit verde.

**Commit C-resto-(a):** prenter-harness `4ee76db` (`9560154..4ee76db`), pusheado. Memoria [[cockpit-business-tool-campaign]] actualizada (C-resto-(a) cerrado; restan (b) luana / D prenter / E).

## Pendiente E·Fase 3c — Rename del kit brand→sistema (2026-06-28 · ledger I-52 · prenter-harness `dad008d..a826f6f` + prenter `6634372` + chris-corp `6255db4` + PeruSaaS `3a74e58`, pusheados)

**La foto lo enmarcó como "bump MAYOR breaking client-facing" — y la investigación encontró el atajo que lo volvió MINOR.** El cockpit (I-43) YA traducía en su frontera: lee `seam["brands"]` y lo expone como `sistema` (`seamSistemaSlugs`). Formalizar ese puente como **compat-shim** (el loader + los 2 lectores leen `sistemas:` con fallback `brands:`) vuelve el rename **NO-breaking**: un seam pre-rename resuelve para siempre. **Lección:** antes de aceptar un framing de "breaking grande", buscá si ya existe una frontera de traducción que un shim pueda formalizar — convierte un big-bang multi-repo en un cambio incremental y diferible. El operador lo eligió por AskUserQuestion (kit completo · shim · 3 limpios · luana queda).

**El landmine real: "brand" tenía TRES sentidos, no dos — y el agente de exploración subestimó.** El Explore dijo "99.8% nav, solo la voz es marketing". Falso para el `dist/`: ahí vive una **feature de dominio luana** — `brand_audit` (auditar la MARCA del tenant: `BrandAuditService`/`useBrandAuditMutation`/`brand-completeness-score`/`brand_studio`/`brand.md`/columnas `| brand |` de tablas de capability/"Cargar brand state"). Eso NO es la dimensión de navegación; renombrarlo rompe clases/tests y contradice "preservar dominio". **Lección:** en contenido derivado de luana, "brand" es ambiguo (partición-nav vs el módulo-de-negocio "marca del tenant"); un rename mecánico necesita **guarda por-stem** (preservar `brand[-_ ](audit|studio|completeness|expert|voice)` + `brand.md`) + **revisión del diff** + hand-fix de los bare-brand-que-son-módulo (la columna `| brand |`, "Cargar brand state" — el script los mis-renombró, se arreglaron a mano).

**El `dist/` es CÓDIGO, no prosa — y no hay test harness de starters acá.** La maquinaria nav son **vars shell** (`SEAM_BRANDS_RE`/`KNOWN_BRANDS`/`BRAND_R1`/`brand_slug`) que cruzan **13 gates**. Sin tests de starter que corran en la fábrica (el vitest es del cockpit-ui). **Cómo se verificó sin tests:** rename **global-consistente** (toda definición + uso de un token se renombra junto → consistencia por construcción, sin huérfanos) + `bash -n` en todos los `.sh` + **scan de huérfanos** (`grep` de las vars viejas = vacío). `brand.yaml` (config per-sistema = nav) → `sistema.yaml`; `brand.md` (doc del módulo = dominio) → preservado.

**Dos trampas de regex/idioma:** (1) **"nombrando" contiene "brand"** como substring (`no-m-brand-o`) → el renombrador usó **no-precedido-por-letra** (`(?<![A-Za-z])brand`, agarra `_BRANDS`/`{brand}` pero NO el substring) — pero los greps de VERIFICACIÓN (substring) **sobre-reportan**, hay que filtrarlos. (2) **Género ES**: "brand" se usaba femenino ("otra brand", "las brands", "brand propia") y `sistema` es masculino → rename mecánico rompe concordancia ("otra sistema") → **pase de género** (otra→otro, las→los, propia→propio…) + revisión.

**Migración de seams hermanos = shim BIDIRECCIONAL + copias prístinas.** Hacer `_SEAM_ALIASES` mapear `sistemas↔brands` en **ambos** sentidos permite flipear la key del seam a `sistemas:` **sin tocar cada script que aún pide `brands.active`** (expand-contract). Las copias del kit de prenter/chris-corp/PeruSaaS estaban **prístinas** (diff vs pre-sesión = idéntico) y sus gates leen el **registry**, no el seam → el cambio mínimo fue **cp del loader bidireccional + rename de la key** (2 archivos por repo). **luana NO se migró**: su seam = 23.559 occ de su producto nativo = des-luana-ficación, otra campaña; el shim lo puentea para siempre (igual que cualquier cliente sin migrar). **Lección:** un shim bidireccional desacopla el orden de migración — cada consumidor migra cuando quiere, ninguno rompe.

**El cambio de data-shape estaba en un camino DORMANT.** `sistemasIn→seamSistemaSlugs` solo surte algo si `<slug>/docs/product` existe; ninguno de los 3 repos migrados lo tiene (usan platform-pseudo/workspace externo) → la migración es no-breaking **incluso en el binario viejo en memoria**; el dual-read es por corrección, con cero efecto visible. **Lección:** rastreá el caller antes de asumir que un cambio de forma de dato es riesgoso — puede estar en un camino muerto.

**Toolchain (confirmado, sin sorpresa):** build UI = next local + **trap restaura `app/api` con ruta ABSOLUTA ANTES del `cd`** (lección F4) — limpio; rebuild binario + restart del daemon **desde chris-corp** (`make -C ~/Proyectos/chris-corp cockpit-restart`, home base, `.cockpit/` ahí). **Smoke API (curl) suficiente** para un cambio de capa-dato sin delta visual: portfolio 5 empresas, factory ledger **52 fichas** (I-52 live), **vitalia que queda en `brands:` carga 200** (el binario nuevo lee el seam legacy). El skill `ledger` registró I-52 (trampa del next-id otra vez: `grep I-5` pega I-52/I-53 en el placeholder; última ficha REAL = I-51). 6 commits en prenter-harness (uno por slice S1-S5 + shim bidireccional), 1 por adopter.

## C-resto-(b) — limpieza de prosa stale del launcher en luana (2026-06-28)

**El handoff puso el cwd MAL — y el cwd mal es una trampa silenciosa.** El prompt decía `cwd = ~/Proyectos/luana-platform`; la sesión real arrancó en `~/Proyectos/luana-vitalia` (worktree de marca, `wip/vitalia`). El operador dijo "haz todo desde aquí". **Pero vitalia estaba BEHIND main** → todavía tenía los scripts del launcher presentes (`scripts/cockpit-daemon.sh`, `cockpit-up.sh`) que en main ya habían migrado a chris-corp. Editar la prosa en vitalia = editar contra un baseline viejo + el cambio se clobberea en el próximo sync. **Lección:** "desde aquí" no significa "en la branch de aquí" — el SSoT de la prosa cross-cutting es **main**; se edita en main y se propaga down. Confirmar HEAD de CADA worktree (`merge-base --is-ancestor main HEAD`) antes de elegir dónde tocar.

**El handoff es FOTO — re-verificar destapó 2 cosas que la foto entendía mal/de menos.** (1) El env override real del daemon de chris-corp es **`COCKPIT_BIN`**, no `ALPACA_COCKPIT_BIN` — la prosa de luana estaba directamente equivocada, no solo "vieja". (2) El daemon real hace `-port` y nada más: **no** `DEFAULT_BRAND`, **no** `WORKSPACE_ROOT`, **no** `-workspace`, **no** per-brand-port. Toda esa "cómo funciona" de la §Cockpit describía un launcher MUERTO → no era "cambiar el comando", era reconciliar el modelo. **Lección:** grepeá la interfaz real (el script destino) antes de renombrar env-vars en prosa — renombrar a un nombre que el script no lee es un bug nuevo disfrazado de limpieza.

**El gate de cierre de story bloqueó commitear autosave ajeno → stash, no override.** Los 2 archivos dirty pre-existentes (`mateo-vista-semana` checkpoint+chris-input) comparten módulo `scheduling` con una story open (`mateo-nueva-cita`) → el pre-commit de luana (story-closure Layer 4) BLOQUEA el commit. Override (`STORY_CLOSURE_GATE_SKIP=1`) era posible pero sucio (commitear WIP de otro contexto bajo un override). **Solución limpia: `git stash push -- <2 files>` → sync → push → `git stash pop`.** Los merge commits NO disparan el gate (comunify/nicolify mergearon main con stories open sin drama) → stashear destraba el merge sin tocar el contenido. Verificado: locks de sesión STALE (jun 2-26, hoy 28) → sin sesión viva → stash seguro.

**Dos trampas de tooling de la propia sesión:** (1) El `Edit` tool exige un `Read`-tool previo del archivo — leer con `sed`/`grep` (Bash) NO cuenta → "File has not been read yet". (2) `git add a b c` **aborta el batch entero** si UN pathspec no matchea (ej. el path viejo post-`git mv` ya no existe) → nada se stagea. Stagear el path NUEVO + dejar que el `git mv` ya haya staged el rename.

**`sync-from-main.sh` sincroniza contra `origin/main`, no contra el main local.** El commit vivía en el main local (worktree luana-platform); el script hace `git fetch origin main`. → **push main→origin PRIMERO**, después el script propaga a cada wip. Orden: commit-en-main → push main → por cada wip: `sync-from-main.sh` (real merge, trae 2 commits: I-48 + este) → push wip. Legacy (`legacy/nicolify-original`) NO se sincroniza (branch congelado por diseño).

## D — unificar `crear-pieza-comercial` (2026-06-28 · H-04)

**El framing de la FOTO estaba mal en lo load-bearing, no solo viejo.** El handoff decía "~17 líneas de voz/branding legítima por-empresa → preservar como overlay" y marcaba prenter como "SSoT candidato". Re-verificar (diff propio) destapó: vitalia/comunify/nicolify son **byte-idénticas entre sí** y su drift vs prenter es **100% versión pre-I-39** (cero voz legítima); PeruSaaS no era "257 líneas customizadas" sino un archivo de **60 líneas** = el motor genérico ya extraído a mano. Y la versión de prenter era la **MÁS cargada de cosas prenter-específicas** (Mateo, IDs de Claude Design, persona, H-12) — el peor candidato a "núcleo limpio". **Lección:** re-verificá el FRAMING del handoff, no solo los hechos; "la copia del dueño = el núcleo" puede estar al revés (era el overlay más pesado). El núcleo limpio ya existía en el lugar menos esperado (PeruSaaS).

**El hogar que el operador eligió no tenía la maquinaria que la decisión asume → FRENAR y re-preguntar.** "SSoT = chris-corp + sync" asumía un sync que **no existe** (las 5 copias se esparcieron a mano — esa ES la causa del drift), un dominio `marketing/` que chris-corp **no tiene** (sus dominios = finanzas/portfolio/legal), y un ledger que **no es** `ledger.yaml` (chris-corp registra en `decisiones.md` por dominio / `harnesses/DECISIONS.md` H-NN). **Lección:** cuando una decisión nombra un hogar, **verificá que ese hogar tenga la maquinaria** (sync · dominio · libro de actas) ANTES de ejecutar; si falta, es scope nuevo → re-preguntá (acá: ¿construir el sync ahora o diferirlo? cómo adjuntar el overlay?).

**El seam YA estaba diseñado para esto — leer el doc del loader antes de inventar canal.** `harness_config.py` documenta 4 clases de consumidor; la **class D = markdown: `{slot}` CONVENTION (el modelo Lee el yaml) — sin loader call**. O sea: una skill leyendo `marketing.*` del seam es un consumidor de primera clase previsto, no un hack. `--doctor` deep-walkea por `__FILL_ME__` → slots de overlay sin llenar se reportan solos (intencional). **Lección:** revisá las clases de consumidor que el loader ya soporta antes de diseñar un mecanismo de overlay nuevo.

**Trampa de despliegue: el SHELL ≠ el worktree de app.** `portfolio/registry.yaml` tiene `empresas[].repo` = el shell de marketing (`~/Proyectos/vitalia`) pero `cockpit.path`/`workspace` = el worktree del SaaS (`~/Proyectos/luana-vitalia/vitalia`). El sync del skill de **marketing** debe ir al **shell** (`repo:`), NUNCA al worktree de app. El operador lo flagueó ("cuidado dónde se despliegan"). **Lección:** cuando una empresa tiene shell + worktree separados, fijate cuál campo del registry corresponde a la naturaleza del artefacto (marketing→shell, producto→worktree).

**"Técnicas inline" NO orfanó limpiamente `methods/registry.yaml`.** La decisión removió la dep del SKILL, pero el archivo lo siguen referenciando `marketing/CLAUDE.md`, `marketing/README.md`, un deal real de Prospera, y **`repo-map.yaml` lo trackea** (borrarlo rompería el gate de coherencia). → NO se borró: queda como **doc de dominio**, el skill solo dejó de depender de él. **Lección:** antes de borrar un archivo "ahora sin uso", grepeá **todos** los referrers + el repo-map; "el skill ya no lo lee" ≠ "nadie lo usa".

**La unificación destapó un drift latente de path (I-39).** El viejo SKILL.md de prenter ponía la pieza bespoke en `clientes/<x>/proyectos/<y>/comercial/`, pero el modelo I-39 (+ `ports.yaml` + el deal real de Prospera, que vive en `deals/`) dice `clientes/<x>/deals/<y>/comercial/` (nuestro pitch, pre-firma, company-side; `proyectos/` es el repo-objeto del cliente, gitignored). El `bespoke_home` del seam quedó con el `deals/` correcto; la prosa de `marketing/CLAUDE.md` sigue con `proyectos/` → flagueado como deuda aparte (no se persiguió, scope discipline). **Lección:** extraer/genericizar un artefacto es un buen momento para que afloren drifts latentes — anotalos, fijá el valor correcto en el lugar que tocás, y deferí el resto.

**Disciplina que funcionó:** 4 rondas de AskUserQuestion antes de tocar (el operador refinó el corte cada vez: chris-corp como SSoT, no el kit · seam como canal · bespoke_home como puerto abstracto hexagonal · técnicas inline). Commits por pathspec, 1 por repo (chris-corp `6b4da38` SSoT+sync+H-04 · prenter `d4678bb` consumidor). El `Edit` tool exige `Read`-tool previo del Makefile (bash `cat` no cuenta — misma trampa de C-resto-(b)).
