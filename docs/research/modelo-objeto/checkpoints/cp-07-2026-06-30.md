# cp-07 · 2026-06-30 — arranca el dogfood real (M4): topología + método + fresh start

## Qué se hizo
Arrancó M4 (dogfood). La sesión fue casi toda **corrección de rumbo del operador** (3 casi-desvíos) →
4 decisiones clavadas (D-14..D-16) + fresh start ejecutado en el repo `prenter`. NO se empezó a poblar aún:
el operador vuelca su conocimiento la próxima (D-16).

## Casi-desvíos corregidos (el valor de la sesión)
1. **`cuenta_ref` cross-repo** → era artefacto de la laptop del operador (prenter + prenter-harness hermanos),
   no arquitectura. La arista cuenta→proyecto vive en el registry (control plane). **D-14** + guardrails
   (test de norma · disciplina de planos · orden de autoridad). Memoria: `guardrail-norma-no-instancia`.
2. **Marketing colado al objeto** (leí `positioning.md` para "qué hace Prenter") → marketing = otro concern,
   otra carpeta, otras skills. FUERA. Memoria: `foco-un-concern-flag-explicito`.
3. **Sobre-apliqué D-01** (metí todo el objeto en prenter-harness) → prenter-harness es solo un SISTEMA;
   la EMPRESA es prenter. **D-15** (topología).

## Decisiones
- **D-14** · objeto autocontenido, anclado al resultado final data-plane; sin `cuenta_ref`; 3 guardrails.
- **D-15** · topología: prenter-harness=SISTEMA, prenter=EMPRESA. Objeto de negocio → `prenter/empresa/<tipo>/`
  (plano). Data del sistema → repo prenter-harness (ref por slug). negocio.yaml que regenera = el de prenter.
- **D-16** · método de llenado: operador VUELCA (texto plano) → Claude CARTOGRAFÍA a entidades (no deriva) →
  operador CORRIGE → itera. Objetivos se crean JUNTOS. Provenance = Declarado.

## Ejecutado (repo prenter, reversible)
- `git mv` empresa.yaml + negocio.yaml viejos → `prenter/empresa/_deprecated/` (+ DEPRECATED.md). CLAUDE.md/decisiones.md intactos.
- Nueva raíz delgada `prenter/empresa/empresa.yaml` (id prenter, Lima, tax_id PENDIENTE).
- `prenter/empresa/objetivos/` vacío (placeholders de hipótesis borrados).

## Nota de infra
El folder de la campaña se movió `~/Proyectos/` → `~/Documentos/campaign-modelo-objeto/` (durante la sesión). Todo intacto.

## Próximo
El operador pega el volcado de Prenter → Claude lo mapea a las 9 entidades → iterar → regenerar negocio.yaml (prueba). Ver `NEXT-PROMPT.md`.
