# CP-05 · 2026-06-30 — MATERIALIZACIÓN v1 (objeto.schema.yaml)

**Macropaso:** M1 → **modelo clavado y materializado**. Hito mayor.

## Qué se hizo
1. **Investigación 4-subagentes** (cp-04) → `ESTRUCTURA-INICIAL.md` → forks A–G confirmados por el operador.
2. **Paréntesis SOMA Act** (herramienta entregada a Prospera): usa SIPOC + tortuga/ISO 9001 + RACI + Bloom +
   APQC → **valida D-08**. Adoptado: `actividad.verbo`(Bloom)+`tiempo`, `proceso.documentos`. SOMA = profundo
   en la ficha, sin capa estratégica (Hilo/capability) → nosotros más anchos.
3. **MATERIALIZADO** `.claude/harness/schema/objeto.schema.yaml` (L0, **aditivo** — no toca el `negocio.yaml`
   vivo; `negocio` = su proyección, D-06). Validado YAML. **Ejemplo** `ejemplo-vertice.yaml` validado
   end-to-end (parse + integridad de refs + RACI A==1; el checker cazó el ref colgante a propósito).

## D-11 (clavada) — la v1
8 nodos anclados a **O1-O7 + ArchiMate**, `met:` por campo, provenance `fuente·conf` obligatorio, Hilo de Oro
= 8 relaciones (un-extremo + inverso por scan), invariantes FK. Forks aplicados:
A O1-O7 · B split objetivo/KR · C unificar gap (negocio.brecha=vista) · D persona/rol net-new ·
**E `audiencia`→`sirve_a`** · F sistema=una App Component(+Product) · G capability-negocio≠producto.
Principios: **alta cohesión** (1 archivo=1 aggregate) / **bajo acoplamiento** (FK, nunca embeber root, inverso
por scan, schema aditivo). v1 para iterar rápido.

## Colisiones / micro-opens cerrados
- `audiencia`→`sirve_a` (fork E). · `persona:rol` N:M soportado (`persona.roles[]`). · Bloom = candidato M32.

## Estado de artefactos
- **Repo:** `.claude/harness/schema/objeto.schema.yaml` (validado). [aún sin commitear]
- **Campaña:** ESTRUCTURA-INICIAL.md · ejemplo-vertice.yaml · GLOSARIO (audiencia resuelta) · MODELO/DECISIONES/INDICE al día.

## Siguiente (Pregunta 6 — elegir)
1. Guía `MODELO-NEGOCIO.md` en products/ (grounding de la ingesta) · 2. Dogfood Proyecto #0 in-repo ·
3. Cablear proyección + RepositorioEmpresa + validador Go (M5) · 4. Ledger I-NN (D-08..D-11) + ficha M32 Bloom.
Fondo: D-06 versionado, D-07 holding, M6 visión macro→micro.
